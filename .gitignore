# ── رازها (امنیت — هرگز) ──
.env
.env.*
.cf
secrets/
*.pem
*.key
*.crt
.git-credentials
**/rclone.conf
certbot/
# ── بازتولیدشونده ──
**/node_modules/
frontend/prochart/dist/
frontend/prochart/public/crypto-icons/
__pycache__/
*.pyc
*.pyo
logs/
*.log
backups/
backup-logs/
# خروجیِ خودکارِ qa/python/run-clean.sh — trace/گزارشِ Playwright، چند گیگابایت
artifacts/
app/artifacts/
SHA256SUMS
# ── محتوا/باینریِ سنگین (روی سرور می‌مانند؛ گیت جای باینریِ بزرگ نیست) ──
avatar_work/
samples/
ig_data/
ig_media/
ig-worker/
media/
edu_assets/
ml_models/
video_studio/public/
chart_service/node_modules/
*.mp4
*.pth
*.PNG
*.JPG
*.JPEG
*.bak.*

# ── بخش‌های coinepro fx ──
# پیش‌تر کنار گذاشته می‌شدند چون در دیپلویِ pro-chart استفاده نمی‌شوند؛ اکنون به
# خواستِ مالک کلِ سورس روی گیت‌هاب می‌رود. فقط node_modules/dist (بازتولیدشونده،
# بالا ignore شده‌اند) بیرون می‌مانند — سورسِ خودشان روی‌هم ~۵ مگابایت است.
video_studio/public/
# CLAUDE.md دیگر ignore نیست — راهنمای Claude Code روی سرورِ مرکزی است (به docs/SERVER-HANDOFF.md اشاره می‌کند)
Anjoman Max SemiPro.zip
