SHA256SUMS covers every regular artifact file except SHA256SUMS itself and
manifest-verification.log. The latter is generated by `sha256sum -c SHA256SUMS`
after the manifest is sealed, so including it would create a recursive mutable
output. A zero-failure verification transcript is stored in that excluded log.
