# برنامهٔ فازبندی — رساندنِ بازارنما به انسجامِ TradingView

**مبنا:** [`TV_GAP_REGISTER.md`](TV_GAP_REGISTER.md)
**دامنه:** بصری + درستیِ کارکردی. **خارج از دامنه:** پوششِ نماد و صرافی (تصمیمِ استراتژیک، با کد بسته نمی‌شود).
**روش:** بدونِ لوپ. چند فازِ بسته با معیارِ پذیرشِ مشخص.

> **به‌روزرسانیِ ۲۰۲۶-۰۷-۱۷:** مالک تأیید کرد که مسئلهٔ حقوقیِ آیکون‌های TradingView حل و توافق شده است. بنابراین V-1 از «جایگزینیِ اجباری» به «تکمیل و یکدست‌سازی» تغییر کرد. یافتهٔ فنی (دو سیستمِ ناهماهنگ + دو آیکونِ خراب) پابرجاست.

---

## فاز ۰ — دروغ نگفتن
**چرا اول:** رابطی که عددِ غلط می‌دهد از رابطِ ناقص بدتر است. یک تریدر ممکن است روی آن معامله کند.
**وابستگی:** هیچ.

| # | تسک | فایل | تلاش |
|---|---|---|---|
| 0.1 | هشدارِ اندیکاتور: یا `cond.type` را از `form.source` بساز، یا گزینه را **غیرفعال کن** | `AlertsPanel.jsx:268`، `src/bazaarnama/tasks.py` | M |
| 0.2 | هشدارِ ترسیم: مختصات ذخیره کن یا گزینه را حذف کن | `AlertsPanel.jsx:302` | M |
| 0.3 | کانال‌های تحویل: یا ارسال را پیاده کن (اول وبهوک) یا فقط تلگرام را نشان بده | `tasks.py:172` | M |
| 0.4 | `mfi` روی ابزارِ بدونِ حجم: `null` برگردان نه ۱۰۰ | `indicators.js:380` | S |
| 0.5 | `vwap` بدونِ حجم: `null` یا برچسبِ صریحِ HLC3 — نه VWAPِ جعلی | `indicators.js:120` | S |
| 0.6 | ایچیموکو: جابه‌جاییِ ۲۶ کندل + Chikou | `indicators.js:270` | M |
| 0.7 | `mtfEma`/`mtfRsi`: سوگیریِ lookahead — کندلِ بسته نباید با کلوزِ آینده پر شود | `indicators.js:212` | M |
| 0.8 | دراپ‌داونِ منبع روی ۱۰ اندیکاتور: `Array.isArray('high')` همیشه false | `indicators_ext_a.js:273` | S |
| 0.9 | رابطِ مرده را پنهان کن: `evDividends`، `evSplits`، `scaleUnit`، «کپیِ لینکِ تصویر» (`bnSnapshotUpload` **تعریف نشده**)، brush/highlighter | چند فایل | S |
| 0.10 | ۶ تصادمِ رجیستری را صریح کن (الان `Object.assign` بی‌صدا برنده می‌شود) | `indicators.js:554` | S |

**پذیرش:** هیچ رابطی قابلیتی را ادعا نکند که موتور ندارد. تستِ رگرسیون برای ۰.۴ و ۰.۵.

---

## فاز ۱ — بردهای یک‌خطی
**چرا:** بیشترین تغییرِ محسوس به‌ازای کمترین کد.

| # | تسک | فایل | تلاش |
|---|---|---|---|
| 1.1 | `chipBg: 'transparent'`، `chipBgHover` عمیق‌تر (~`rgba(0,0,0,.08)`) | `BazaarNama.jsx:277` | **S** |
| 1.2 | `brand.*` → توکن‌های AAِ موجود (`upText #086d5c`، `downText #b42335`، `accentText #1e53e5` قبلاً ساخته و تأییدشده‌اند) | `tailwind.config.js:8` | S |
| 1.3 | `--text-muted` تیره‌تر (≥`#6b6e78` روشن) — الان ۴.۲۲ و در ۹px استفاده می‌شود | `index.css` | S |
| 1.4 | سایه: ۵+ → یکی | `index.css` | S |
| 1.5 | `MIN = 1500` → `0`، fade به ~۲۰۰ms → **~۱۵۰۰–۱۹۰۰ms بهبودِ ادراکی** | `App.jsx:111` | **S** |
| 1.6 | brotli روشن (الان gzip با نسبتِ ۲.۷۹×) → **−۳۹۰KB** | nginx / CF | S |
| 1.7 | `cf-cache-status: DYNAMIC` روی هر asset — کلاودفلر هیچ‌وقت کش نمی‌کند | CF | S |
| 1.8 | `logo.png` ۱۲۵۴×۱۲۵۴ / ۴۱۶KB → نمایش در ۱۲۸×۱۲۸ (**۹۶× اضافه‌کشی**) | asset | S |

**پذیرش:** اسکرین‌شات‌دیفِ قبل/بعد. لایت‌هاوس روی پروداکشن.

---

## فاز ۲ — یک سیستم به‌جای چهار
**چرا:** تا این نباشد، هر اصلاحِ بصریِ بعدی دوباره واگرا می‌شود.
**وابستگی:** فاز ۱.

| # | تسک | تلاش |
|---|---|---|
| 2.1 | `--pc-*` تنها بازمانده. `--fs-*` و `--r-*` حذف | M |
| 2.2 | `TH` (`BazaarNama.jsx:262`) و `brand.*` از `--pc-*` بخوانند | M |
| 2.3 | `h-9` grep-ban → ۸ ارتفاعِ دکمه `[20,24,25,28,29,30,33,36]` به **۳** برسد (فردها = نیم‌پیکسل = تار) | M |
| 2.4 | رمپِ ۱۱ پله‌ایِ خودمان با پله‌های ثابتِ L* — **نه کلونِ TV** (پالتِ فعلی، پالتِ بازنشستهٔ TV است) | M |

**پذیرش:** یک منبعِ حقیقتِ توکن. لینتِ CI که `h-9` و رنگِ hex-hardcoded را رد کند.

---

## فاز ۳ — پایه‌های بصری
**وابستگی:** فاز ۲.

| # | تسک | تلاش |
|---|---|---|
| 3.1 | تمِ روشن وارونه: صفحه ~`#ebebeb`، پنل `#ffffff` (الان پلهٔ ۱.۰۵ = نامرئی) | M |
| 3.2 | ۳۹ واشِ آلفا → توکنِ توپر (آلفا تودرتو نمی‌شود: ۰.۰۴ روی ۰.۰۴ = ۰.۰۷۸) | M |
| 3.3 | کفِ **۱۱px** برای متنِ فارسی. اعشار (۱۱.۵، ۹.۵) ممنوع. الان `9px/16px` در ۲۴ جا | M |
| 3.4 | هر کنترل به `--pc-font-*` + `--pc-line-*` بایند شود | S |
| 3.5 | `IRANYekanX` به `tailwind.config.js` اضافه، سپس `*` به `html, body` محدود شود (الان تلهٔ نهفته) | S |
| 3.6 | لجندِ موبایل زیرِ چیپ‌های BUY/SELL می‌رود + سرریزِ RTL | S |

**پذیرش:** ماتریسِ کنتراستِ axe سبز. اسکرین‌شات در ۳۹۰ و ۱۴۴۰، لایت و دارک.

---

## فاز ۴ — آیکون و حرکت
**وابستگی:** فاز ۲.

| # | تسک | تلاش |
|---|---|---|
| 4.1 | اسپکِ آیکون: ۲۸×۲۸، مویی ۱px، butt cap، filled even-odd، پدِ ۲px | S |
| 4.2 | `Eye` را درست کن — الان **شِوران** است نه چشم، در فضای ۱۸ واحدی داخلِ viewBoxِ ۲۸ | S |
| 4.3 | `Search` — مسیرِ سرگردانِ رعدوبرق را بردار | S |
| 4.4 | بقیهٔ آیکون‌ها را کامل کن تا `export * from 'lucide-react'` حذف شود — **یک سیستم، نه دو** | M |
| 4.5 | کاراتِ ابزار (▾) روی هاور، نه همیشه | S |
| 4.6 | زمان‌بندیِ لایه‌ای: رنگ ۶۰ms (تأییدِ فوری)، پس‌زمینه ~۳۰۰ms (نشستِ نرم)، ورود overshoot، خروج هرگز | M |
| 4.7 | `transition: all` ممنوع (روی canvas و input است — پراپرتیِ ناخواسته animate می‌شود) | S |

**پذیرش:** صفر importِ lucide. ماتریسِ حرکتِ QA.

---

## فاز ۵ — پی‌ریزیِ چیزهایی که فرو می‌ریزند
**چرا:** این‌ها هنوز نترکیده‌اند ولی خواهند ترکید.

| # | تسک | تلاش |
|---|---|---|
| 5.1 | **ترسیم‌ها per-symbol** — الان یک آرایهٔ سراسری؛ هر ترسیم روی هر نماد رندر می‌شود | M |
| 5.2 | سقفِ کندل + downsampling — اسکرولِ چپ **بی‌کران** ۵۰۰تا اضافه می‌کند و همهٔ اندیکاتورها را روی کلِ آرایه بازمحاسبه می‌کند | M |
| 5.3 | `accelerator` هستهٔ O(n²) — SMA داخلِ `.map()` | S |
| 5.4 | undo برای `clearAll`/`setDrawings` — الان لودِ چیدمان برگشت‌ناپذیر است | S |
| 5.5 | `_hit` فیلدِ `visible` را چک نمی‌کند — شیِ مخفی هنوز کلیک‌پذیر است | S |

---

## فاز ۶ — کدِ روی طاقچه (بردِ ارزان)
**نوشته شده، هرگز وصل نشده. تقریباً همه‌اش S.**

| # | تسک | شاهد |
|---|---|---|
| 6.1 | APIِ قالبِ اندیکاتورِ `layoutPresets.js` | ~۱۵۰ از ۲۷۰ خط **صفر call site** |
| 6.2 | `EXT_FREEHAND` — brush/highlighter را زنده می‌کند | `drawtools_ext.js:866`، **هرگز import نشده** |
| 6.3 | intrabarِ بازپخش | `ReplayController.js:219-256`، **صفر فراخوان** |
| 6.4 | `perf.js` — ۱۶۵ خط، هیچ‌جا import نشده (rafThrottle برای فاز ۵ لازم است) | grep = ۰ |
| 6.5 | اندیکاتورها هنگامِ پخشِ replay فریز می‌شوند — فقط روی seek بازمحاسبه | `:1746` در برابر `:1777` |
| 6.6 | ذخیرهٔ چیدمان `scaleMode`/`sessions`/`tz`/`compares`/`grid` را نمی‌گیرد | `:2167` |

---

## فاز ۷ — چند-چارت
**چرا جدا:** L است و به فاز ۲ و ۵ وابسته.

واردِ هر چیدمانی شوید، **چارتِ واقعی دور ریخته می‌شود** — سلولِ ۰ هم `MiniChart` است (`BazaarNama.jsx:3419`): ~۹۰ خط، یک سریِ کندل، بدونِ اندیکاتور/ترسیم/حجم/بازپخش.

| # | تسک | تلاش |
|---|---|---|
| 7.1 | چارتِ واقعی را به کامپوننتِ سلول ارتقا بده | L |
| 7.2 | `MiniChart`-به‌عنوان-سلول را حذف کن | S |
| 7.3 | `SYNC_DIMENSIONS.interval` مرده است | S |
| 7.4 | همگام‌سازیِ ترسیم بینِ سلول‌ها | M |

---

## خارج از این برنامه — تصمیمِ استراتژیک

| موضوع | چرا کد حلش نمی‌کند |
|---|---|
| **پوششِ نماد** | ~۶۹۰ در برابرِ ~۱M. لایسنس و قراردادِ فروشنده. |
| **نمااسکریپت v۲** | زبان نیست؛ `new Function` + ۵ regex. تعمیرِ تدریجی‌پذیر **نیست** — پارسرِ واقعی می‌خواهد (فصل‌ها). |
| **انتشارِ اسکریپت** | اثرِ شبکه‌ایِ ۱۰۰هزار اسکریپتِ TV. وابسته به بالایی. |

این سه، «فاصلهٔ بصری» نیستند و در ۴ فاز تمام نمی‌شوند. باید جداگانه تصمیم گرفته شوند.

---

## معیارِ موفقیت

**نه** «تغییر اعمال شد». لوپِ قبلی ۲۱۷ بار همین را کرد و فقط ۶ الزام VERIFIED شد — و ۱۵۰ خطِ `layoutPresets.js` نوشته و هرگز وصل نشد.

هر فاز وقتی تمام است که:
1. اسکرین‌شات‌دیفِ قبل/بعد در ۴ حالت (۳۹۰/۱۴۴۰ × لایت/دارک)
2. تستِ رگرسیون برای هر باگِ فاز ۰
3. الزامِ متناظر در `REQUIREMENTS_STATUS.csv` به **VERIFIED** برود — نه `BASELINED`
