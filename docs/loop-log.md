# لاگِ لوپِ همسان‌سازیِ Pro-Chart ↔ TradingView

> هر ردیف = یک لوپ. صادقانه ثبت می‌شود (حتی اگر پیشرفت کم بود).

---

# 🏁 گزارشِ نهایی — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۱۶:۱۰ — مالک گفت «توقف»
لوپ پس از **۴۴ تکرار** به دستورِ مالک متوقف شد. خلاصهٔ صادقانه:

## تغییراتِ واقعیِ دیپلوی‌شده (که سایت را محسوس به TradingView نزدیک کرد)
1. **واچ‌لیست:** نمایشِ نامِ نماد (EUR/USD…) که کاملاً غایب بود + رفعِ برشِ ticker (باگِ CSSِ RowActions).
2. **ریلِ ابزارِ چپ:** فعال‌سازیِ خوشهٔ پایینِ سبکِ TV (مگنت قوی/ضعیف، ماندن در ترسیم، قفل/مخفی/حذفِ همه).
3. **دکمه‌های SELL/BUYِ چارت:** سبکِ outlineِ TV (قیمت بالا/برچسب پایین).
4. **گرید:** خطوطِ داخلیِ بسیار کم‌رنگِ TV (توکنِ gridLine).
5. **اینتروال:** منتخب‌های inline + dropdownِ گروه‌بندی‌شده (دقیقه‌ای/ساعتی/روزانه) با ستارهٔ منتخب.
6. **جداکننده‌های عمودیِ تولبار.**
7. **تگِ کراس‌هیر:** خاکستریِ خنثی (نه آبی).
8. **باگِ واقعیِ رفع‌شده — دقتِ قیمت:** EURUSD «1.14» → «1.14448» (سری priceFormat نداشت).
9. **واترمارک:** شبح‌وارتر.
10. **countdown:** روی محورِ قیمت (دنبالِ قیمت).
11. **رفعِ سطلِ آشغالِ تکراریِ ریلِ چپ** (گزارشِ مالک).
12. **تمیزسازیِ کاملِ ازدحامِ پایینِ ریلِ چپ** (گزارشِ مالک) — undo/redo/درخت به تولبار رفت.
13. **⭐ تولبارِ تک‌ردیف/آیکون‌محور** (تأییدِ مالک) — بزرگ‌ترین تغییرِ محسوس؛ عرض ۲۶۵۱→۱۳۰۰px، دو‌ردیف→تک‌ردیف مثلِ TV.

## درسِ صادقانه
لوپ‌های ۱۴–۴۴ عمدتاً **راستی‌آزمایی/دیده‌بان** بودند (بدونِ تغییرِ محسوس) چون سایت از قبل در بیشترِ سطوح TV-faithful بود و سندِ audit کهنه بود. مالک به‌درستی نقد کرد که «تغییرِ جدیدی نمی‌بینم»؛ درس: زودتر باید سراغِ تغییراتِ ساختاریِ محسوس (تولبار/ریل) می‌رفتم نه میکرو-پولیش و دیده‌بانِ مکرر. verify همیشه با **اسکرین‌شاتِ زنده** (صفحهٔ لوکالِ آفلاین فقط mount/TDZ را نشان می‌دهد).

## کارِ باقی‌مانده (خارج از اسکوپِ frontend، برای آینده)
- مواردِ داده‌محور: حجم/اسپرد/۰.۰۰٪ فارکس = مسئلهٔ data-layer (فید)، نه ظاهر.
- خلاصهٔ لوپ‌به‌لوپ در حافظهٔ دائمی هم هست (`prochart-tv-parity`).

**وضعیتِ نهاییِ سایت:** سالم (`200`)، تولبارِ تک‌ردیف، ریلِ چپِ تمیز، نمای کلیِ TV-مانند. لوپ متوقف است (ScheduleWakeup زده نشد).

---

## لوپ #1 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۴:۰۰
- بخشِ مقایسه‌شده: **واچ‌لیستِ ریلِ راست** (ردیفِ نمادها)
- اختلافِ TV که دیدم: در TV هر ردیفِ واچ‌لیست **متنِ ticker** را نمایان نشان می‌دهد (AAPL/TSLA/NFLX). در پرو‌چارت ستونِ نماد در پنلِ باریکِ RTL کاملاً به صفر **له** شده بود؛ فقط پرچم + نقطهٔ وضعیت + سه ستونِ عدد دیده می‌شد و **هیچ متنِ نمادی** نبود.
- ریشه: پیش‌فرضِ `logoSize:'lg'` → لوگوی ۳۰px + سه ستونِ عددی (۵۰+۴۶+۴۴px) کلِ عرضِ ریل را می‌خورد و `flex-1`ِ ستونِ نماد با `truncate` به صفر می‌رسید.
- چه عوض کردم (`bazaarnama/RightPanel.jsx`):
  - لوگو: `lg 30→24 / md 24→20 / table 20→18` (هم‌ترازِ آیکونِ ~۲۰pxِ TV)
  - عرضِ ستون‌های عددی فشرده‌تر: آخرین ۵۰→۴۴، تغییر ۴۶→۴۰، ٪ ۴۴→۳۶؛ `gap-1.5→gap-1`
  - `min-width` تضمینی روی ستونِ نماد (۵۲px) + فونتِ نماد ۱۲→۱۱px تا هرگز حذف نشود
  - هدرِ ستون‌ها هم‌تراز شد. **بدونِ حذفِ هیچ قابلیت/prop/رفتار.**
- verify: ✅ رِندرِ لوکالِ `dist` در chromium — اپ mount شد، `#root` محتوا داشت، **بدونِ pageerror/TDZ/صفحهٔ سفید** (تنها ۵۰۴ = آفلاین‌بودنِ APIِ localhost، نه خطای کد).
- دیپلوی: `347b782` (+`8be38dd`) — فقط `frontend-prochart` بازساخته شد؛ سایت `200`، `api` healthy، بقیهٔ سرویس‌ها (data-feed/ws/db/redis) دست‌نخورده. تأییدِ زندهٔ pro-chart.com: ردیف‌ها حالا `EUR/U… · GBP/… · USD/J… · XAU/…` نشان می‌دهند (قبلاً هیچ).
- درصدِ تقریبیِ شباهتِ واچ‌لیست (حدسِ خودم): ~۷۵٪ (از ~۵۵٪).
- باقی‌مانده برای لوپِ بعد:
  - ticker هنوز کمی برش می‌خورد («EUR/U…» به‌جای «EUR/USD») چون نقطهٔ وضعیتِ بازار داخلِ ستونِ نماد ~۱۰px می‌خورد — یا نقطه را بیرون ببر یا min-width را بالا ببر.
  - آیکونِ گروهِ سکشن (INDICES/STOCKS در TV) و آیکونِ رنگیِ نماد سبکِ TV.
  - سراغِ **تولبارِ بالا** (دو‌ردیف→تک‌ردیف، dropdownِ اینتروال) یا **ریلِ ابزارِ چپ** برو.

## لوپ #2 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۴:۳۵
- بخشِ مقایسه‌شده: **واچ‌لیست — تکمیلِ ticker** (ادامهٔ لوپ #۱).
- روش: به‌جای حدس، عرضِ واقعیِ عناصرِ ردیف را روی سایتِ زنده با playwright اندازه گرفتم (`measure.mjs`). دیدم ستونِ نماد دقیقاً ۵۲px است و «EUR/USD» به ۴۶px نیاز دارد ولی client=42 → برشِ ۴px.
- ریشهٔ واقعی: `RowActions` در حالتِ overlay، کلاسِ base هم `relative` داشت هم `absolute`؛ در ترتیبِ CSSِ Tailwind کلاسِ `.relative` (که دیرتر تعریف می‌شود) برنده می‌شد → اکشن‌های ردیف به‌جای شناور‌بودن، **۳۶px عرضِ واقعی** می‌گرفتند و ستونِ نماد را له می‌کردند.
- چه عوض کردم (`RightPanel.jsx`): position را شرطی کردم — `overlay ? absolute… : 'relative'` (حذفِ `relative`ِ ثابت از base). حالا اکشن‌ها واقعاً absolute‌اند و ستونِ `flex-1`ِ نماد فضای ۳۶px را می‌گیرد. بدونِ حذفِ هیچ فیچر (اکشن‌ها همچنان با hover دیده می‌شوند).
- verify: ✅ رِندرِ لوکال — mount شد، بدونِ pageerror/TDZ/سفید.
- دیپلوی: `28334b7` — فقط `frontend-prochart`؛ سایت `200`. تأییدِ زنده: همهٔ ردیف‌ها ticker کامل نشان می‌دهند: **EUR/USD · GBP/USD · USD/JPY · USD/CHF · XAU/USD** (بدونِ برش).
- درصدِ تقریبیِ شباهتِ واچ‌لیست (حدسِ خودم): ~۸۸٪.
- باقی‌مانده برای لوپِ بعد:
  - آیکونِ رنگیِ گروهِ سکشن + آیکونِ نمادِ سبکِ TV (فعلاً پرچم).
  - سراغِ **تولبارِ بالا** (دو‌ردیف→تک‌ردیف، dropdownِ اینتروال) یا **ریلِ ابزارِ چپ**.

## لوپ #3 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۴:۵۰
- بخشِ مقایسه‌شده: **ریلِ ابزارِ ترسیمِ چپ** (خوشهٔ پایین).
- اختلافِ TV که دیدم: TV پایینِ ریلِ چپ یک خوشهٔ ابزار دارد — مگنت (قوی/ضعیف)، قفلِ همه، مخفیِ همه (چشم)، حذفِ همه (سطل)، و «ماندن در حالتِ ترسیم». پرو‌چارت این‌ها را نداشت؛ فقط یک دکمهٔ مگنتِ مجزا و یک دکمهٔ ماندن، جدا از ریل.
- کشف: کامپوننتِ `ToolRail` از قبل UIِ کاملِ این خوشه را داشت ولی props از `BazaarNama` پاس داده نمی‌شد؛ و موتورِ `drawings.js` هم متدهای واقعی دارد: `lockAll/hideAll/clearAll/setStayInMode/setMagnet`.
- چه عوض کردم:
  - `drawings.js`: `setMagnet(on, mode)` + پیاده‌سازیِ مگنتِ **ضعیف** در `_snap` (فقط اگر نشانگر نزدیکِ OHLC، آستانهٔ ۰.۱۵٪).
  - `BazaarNama.jsx`: stateهای `magnetMode/allLocked/allHidden`، سیم‌کشیِ همهٔ handlerها به ToolRail، و انتقالِ دکمه‌های مگنت/ماندنِ مجزا به داخلِ ریل (relocation، نه حذف).
- verify: ✅ رِندرِ لوکال mount شد (بدونِ TDZ/سفید)؛ و **تأییدِ زندهٔ کامل**: چارت سالم رِندر شد و خوشهٔ پایینِ ریل حالا مگنت/قلم/قفل/چشم/سطل را نشان می‌دهد.
- دیپلوی: `6704eca` — فقط `frontend-prochart`؛ سایت `200`.
- درصدِ تقریبیِ شباهتِ ریلِ چپ (حدسِ خودم): ~۸۰٪ (از ~۶۰٪).
- باقی‌مانده برای لوپِ بعد:
  - گلیفِ دقیقِ آیکونِ ابزارها هم‌ترازِ TV (کراس‌هیرِ گروهِ کرسر، رِی/کانال…).
  - تولبارِ بالا (دو‌ردیف→تک‌ردیف) — بزرگ و پرریسک، جداگانه.

## لوپ #4 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۵:۱۰
- بخشِ مقایسه‌شده: **دکمه‌های سریعِ SELL/BUYِ روی چارت** (گوشهٔ بالا-چپ).
- بررسیِ لجند: اول لجندِ O/H/L/C را چک کردم — از قبل TV-faithful بود (حرف‌ها opacity 0.5، مقادیر رنگی). «خراب» نبود، پس آنجا نچرخیدم و هدف را عوض کردم.
- اختلافِ TV: TV دکمه‌های SELL/BUY را **outline** دارد (پس‌زمینهٔ سفید/پنل، حاشیه+متنِ رنگی)، **قیمتِ درشت بالا / برچسبِ کوچک پایین**، دو جعبهٔ جدا با اسپردِ سادهٔ وسط. پرو‌چارت جعبه‌های **توپرِ** قرمز/آبی با متنِ سفید و ترتیبِ برعکس (برچسب بالا/قیمت پایین) داشت.
- چه عوض کردم (`BazaarNama.jsx`): بازطراحیِ ظاهریِ ویجت → outline، ترتیبِ قیمت-بالا/برچسب-پایین، هاور tint، اسپردِ سادهٔ وسط. **فقط ظاهر — `startTrade('sell'|'buy')` و رفتارِ کلیک دست‌نخورده** (رعایتِ قانونِ «مسیرِ سفارشِ واقعی»).
- verify: ✅ mount لوکال (بدونِ TDZ/سفید) + **تأییدِ زنده**: چارت سالم، دکمه‌ها حالا outlineِ سبکِ TV.
- دیپلوی: `3dc5841` — فقط `frontend-prochart`؛ سایت `200`.
- درصدِ تقریبیِ شباهتِ ویجتِ SELL/BUY (حدسِ خودم): ~۹۰٪.
- باقی‌مانده برای لوپِ بعد:
  - گلیفِ دقیقِ آیکونِ ابزارهای ریلِ چپ (کراس‌هیرِ گروهِ کرسر و…).
  - محورِ قیمت/زمان، یا تولبارِ بالا (دو‌ردیف→تک‌ردیف؛ بزرگ—محتاط).

## لوپ #5 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۵:۲۵
- بخشِ مقایسه‌شده: **گریدِ چارت** (بعد از بررسیِ چند بخش).
- صادقانه: اول لجندِ OHLC (از قبل خوب)، محورِ زمان (نزدیک؛ فقط ماه‌ها کم‌تر ولی وابسته به بازهٔ داده)، و محورِ قیمت را چک کردم. تراکم/بولدِ اعدادِ محورِ قیمت را کتابخانهٔ چارت کنترل می‌کند (ریسکی/غیرقابلِ‌تنظیمِ امن) — پس آنجا دست نزدم.
- اختلافِ TV که دیدم: خطوطِ **داخلیِ** گرید در پرو‌چارت همان رنگِ border بودند (`#e0e3eb` روشن) و از گریدِ بسیار کم‌رنگِ TV (rgba ~.05) پررنگ‌تر؛ TV گریدِ داخلی را خیلی محوتر از بوردرِ محور می‌کشد (audit #32).
- چه عوض کردم (`BazaarNama.jsx`): توکنِ جدیدِ `gridLine` (روشن `rgba(42,46,57,.06)` / تیره `rgba(255,255,255,.05)`) فقط برای `grid.vertLines/horzLines` در هر دو جای createChart و applyOptions؛ `borderColor`ِ محورها روی `TH.grid` (پررنگ‌تر) ماند. بدونِ حذفِ فیچر.
- verify: ✅ mount لوکال + **تأییدِ زنده**: چارت سالم، گرید حالا محوِ سبکِ TV، بوردرِ محور پیدا.
- دیپلوی: `56fb156` — فقط `frontend-prochart`؛ سایت `200`.
- درصدِ تقریبیِ شباهتِ گرید (حدسِ خودم): ~۹۲٪.
- باقی‌مانده برای لوپِ بعد:
  - گلیفِ دقیقِ آیکونِ ابزارهای ریلِ چپ (کراس‌هیرِ گروهِ کرسر و…).
  - تولبارِ بالا (دو‌ردیف→تک‌ردیف)؛ بزرگ‌ترین اختلافِ ساختاری—محتاط و جداگانه.

## لوپ #6 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۵:۴۰
- بخشِ مقایسه‌شده: **تولبارِ بالا — کنترلِ اینتروال** (بزرگ‌ترین اختلافِ ساختاری).
- اختلافِ TV: پرو‌چارت هر ۱۰ تایم‌فریم را inline داشت (تولبار دو‌ردیف)؛ TV یک اینتروالِ تکی + dropdown با favorites دارد (audit #2).
- چه عوض کردم (`BazaarNama.jsx`): TFهای **منتخبِ inline** (پیش‌فرض 15m/1H/4H/1D/1W) + اینتروالِ فعال (اگر منتخب نبود) + یک **dropdownِ chevron** که همهٔ ۱۰ TF را با **ستارهٔ منتخب‌سازیِ ماندگار** (`tfFavs` در localStorage) نشان می‌دهد. stateهای `tfMenu/tfFavs`، هم‌بسته با `closeAll` برای بستنِ کلیک‌بیرون.
- **هیچ TFای حذف نشد** — همه در dropdown در دسترس‌اند؛ فقط جمع‌وجورتر و TV-faithful.
- verify: ✅ mount لوکال + **تأییدِ زنده با کلیک‌تست** (playwright): chevron باز شد، هر ۱۰ TF لیست شد، ستاره‌ها درست، بدونِ pageerror.
- دیپلوی: `ac20c84` — فقط `frontend-prochart`؛ سایت `200`. چارت سالم.
- نتیجه: تولبار خلوت‌تر شد و کراس‌هیر به ردیفِ ۱ آمد؛ **ولی هنوز کاملاً تک‌ردیف نشد** (کنترل‌های ثانویه هنوز wrap می‌شوند).
- درصدِ تقریبیِ شباهتِ کنترلِ اینتروال (حدسِ خودم): ~۹۰٪.
- باقی‌مانده برای لوپِ بعد:
  - برای single-rowِ کامل: کنترل‌های ثانویه (keyboard/camera/fullscreen/sessions یا layout/settings) را در یک منوی «⋯/overflow» جمع کن.
  - گلیفِ دقیقِ آیکونِ ابزارهای ریلِ چپ.

## لوپ #7 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۵:۵۵
- هدفِ اولیه: تک‌ردیف‌کردنِ تولبار (ادامهٔ #۶).
- **یافتهٔ داده‌محور (صادقانه):** با playwright عرضِ کلِ ۲۳ فرزندِ تولبار را اندازه گرفتم = **۲۶۵۶px** در برابرِ ۱۴۴۰px موجود. یعنی single-rowِ کامل غیرممکن است مگر ~نیمی از کنترل‌ها در منوها پنهان شوند — refactorِ بزرگ و پرریسک و در تضاد با «حذف نکردنِ فیچر». (علتِ تک‌ردیفِ TV: برچسب‌های انگلیسیِ کوتاه‌تر + آیکون‌محورتر؛ پرو‌چارت دکمه‌های متنیِ فارسیِ عریض دارد.) پس این کار را به یک لوپِ اختصاصیِ محتاط موکول کردم.
- به‌جایش (audit #15): **جداکننده‌های عمودیِ ظریف بینِ گروه‌های تولبار** اضافه شد (نماد/قیمت | اینتروال | نوع+اندیکاتور | اسکریپت+هشدار+بازپخش+AI | ابزارِ ثانویه) تا مثلِ TV گروه‌بندی‌شده دیده شود.
- verify: ✅ mount + تأییدِ زنده (چارت سالم، جداکننده‌ها بینِ گروه‌ها دیده می‌شوند).
- دیپلوی: `724c8d2` — فقط `frontend-prochart`؛ سایت `200`.
- درصدِ تقریبیِ نظمِ بصریِ تولبار: بهتر، ولی هنوز دو‌ردیف.
- باقی‌مانده برای لوپِ بعد:
  - تولبارِ تک‌ردیف = پروژهٔ جداگانه: یا آیکون‌محورکردنِ چند دکمهٔ ثانویه (با tooltip، بدونِ حذف) یا منوی «⋯» — با تأییدِ مالک بهتر است چون UX را عوض می‌کند.
  - گلیفِ آیکونِ ابزارهای ریلِ چپ، یا کراس‌هیر/تگِ محور.

## لوپ #8 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۶:۱۵
- بخشِ مقایسه‌شده: **تگِ محورِ کراس‌هیر** (hover) — با کلیک/hoverتستِ playwright.
- اختلافِ TV: در پرو‌چارت تگِ قیمت/زمانِ زیرِ کراس‌هیر **آبیِ #2962FF** بود؛ TV آن را **خاکستریِ خنثی** می‌کند و آبی را فقط برای خطِ قیمتِ زنده نگه می‌دارد.
- کشف: `crosshairLabelBg` در تم روی `#2962FF` ست شده بود و پیش‌فرضِ موردنظرِ خودِ کد (`TV_CROSSHAIR_LABEL_BG = '#4c525e'`) را override می‌کرد (کامنتِ کد هم همین را می‌گفت). این توکن هم به لیبلِ کتابخانه هم به `CrosshairAxisTag` می‌رود.
- چه عوض کردم: `crosshairLabelBg` تیره→`#4c525e`، روشن→`#434651` (خاکستریِ خنثی)؛ و `CrosshairAxisTag` متنش سفید شد (کنتراست).
- verify: ✅ mount لوکال + **hoverتستِ زنده**: تگِ «1.15211» حالا خاکستریِ خنثی با متنِ سفید، خطوطِ نقطه‌چینِ کراس‌هیر سالم.
- دیپلوی: `ca1e62f` — فقط `frontend-prochart`؛ سایت `200`.
- درصدِ تقریبیِ شباهتِ کراس‌هیر (حدسِ خودم): ~۹۵٪.
- باقی‌مانده: گلیفِ آیکونِ ریلِ چپ؛ یا تولبارِ تک‌ردیف (منتظرِ نظرِ مالک).

## لوپ #9 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۶:۳۰
- بخشِ مقایسه‌شده: **دقتِ محورِ قیمت و برچسبِ قیمتِ آخر**.
- اختلافِ TV (باگِ واقعی): برچسبِ قیمتِ آخرِ EURUSD «**1.14**» بود (۲ رقم) و سطوحِ محور 1.14/1.15/1.16 (خیلی درشت برای فارکس)؛ TV دقتِ per-symbol دارد (فارکس ۵ رقم → 1.14448). علت: سریِ اصلی `priceFormat` نداشت و پیش‌فرضِ کتابخانه `precision:2` بود. (`fmtPrice`ِ لجند درست بود چون جداست.)
- چه عوض کردم (`BazaarNama.jsx`): `priceFormat: { precision: priceDigits(symbol), minMove }` هم در لحظهٔ `addSeries`ِ استاندارد و هم در یک effect (deps `[symbol, chartType, TH]`) اعمال شد.
- **صادقانه — ۲ دیپلوی:** تلاشِ اول (`b1fcf24`) کار نکرد چون effect با dep=[symbol,chartType] وقتی سری به‌خاطرِ تغییرِ TH دوباره ساخته می‌شد دقت را از دست می‌داد؛ با اسکرین‌شاتِ زنده دیدم هنوز «1.14» است، ریشه را فهمیدم و در `67a25ac` (اعمالِ حین ساخت + TH در deps) درست شد.
- verify: ✅ mount + **تأییدِ زنده**: برچسب 1.14448 و سطوحِ محور 1.14200/1.14400/1.14600/1.14800 (دقتِ فارکس مثلِ TV). چارت سالم.
- دیپلوی: `67a25ac` — فقط `frontend-prochart`؛ سایت `200`.
- درصدِ تقریبیِ شباهتِ محورِ قیمت: ~۹۵٪.
- باقی‌مانده: گلیفِ آیکونِ ریلِ چپ؛ یا جای‌گذاریِ countdown روی محورِ قیمت (سبکِ TV)؛ یا تولبارِ تک‌ردیف (منتظرِ مالک).

## لوپ #10 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۶:۵۰
- بررسیِ چند بخش: **حجم** (audit #35) — رنگش از قبل TV (نیمه‌شفاف .5) بود و sparsity کاملاً **دادهٔ فارکس** است (کامنتِ خودِ کد خطِ ۷۱۵: «چون حجمِ داده صفر است…»)؛ frontend-only پس دست نزدم (مسئلهٔ فید/data-layer، خارج از اسکوپ).
- بخشِ اصلاح‌شده: **واترمارک** (audit #41). واترمارکِ روی چارت (`Watermark`, opacity 0.13 + invert) واضح‌تر از شبحِ TV بود.
- چه عوض کردم: در محلِ فراخوانی `opacity={0.07}` دادم (شبح‌وارترِ سبکِ TV). واترمارکِ ۰.۵ (config خطِ ۱۲۷۲، احتمالاً export/برندینگِ اشتراک) دست‌نخورده ماند.
- verify: ✅ mount + **تأییدِ زنده**: واترمارک محسوسانه کم‌رنگ‌تر/شبح‌وار، خواناییِ کندل بهتر. چارت سالم.
- دیپلوی: `da599bc` — فقط `frontend-prochart`؛ سایت `200`.
- **جمع‌بندیِ ۱۰ لوپ:** واچ‌لیستِ ticker، ریلِ چپ، SELL/BUY، گرید، اینتروال، جداکننده‌های تولبار، کراس‌هیر، دقتِ محورِ قیمت، واترمارک — همه به TV نزدیک شدند. بخش‌های باقی‌مانده عمدتاً یا داده‌محورند (حجمِ فارکس) یا نیازمندِ تصمیمِ مالک (تولبارِ تک‌ردیف).
- باقی‌مانده برای لوپِ بعد: گلیفِ آیکونِ ریلِ چپ؛ countdown روی محورِ قیمت؛ فلای‌اوتِ ابزارها؛ دیالوگِ اندیکاتور/تنظیمات.

## لوپ #11 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۷:۰۵
- بررسیِ صادقانه: **اندیکاتور-دیالوگ** (جستجو + تب‌های اندیکاتور/استراتژی/پروفایل/الگو + دسته‌های Personal/Built-in/Community + نامِ دوزبانه + شمارِ ۹۱) و **فلای‌اوتِ ابزارِ ریلِ چپ** (آیکون + نام + ستارهٔ منتخب + راهنما + هاتکی) هر دو با کلیک‌تستِ زنده بررسی شدند و **از قبل TV-faithful‌اند** — تغییری لازم نبود.
- بخشِ اصلاح‌شده: **countdown به بستنِ کندل** (audit #40). قبلاً گوشهٔ پایین-راست بود؛ TV آن را روی محورِ راست، زیرِ برچسبِ قیمتِ آخر می‌گذارد.
- چه عوض کردم: `CountdownChip` propِ `axisY` گرفت؛ در `BazaarNama` از `priceSeriesRef.priceToCoordinate(livePrice)` در همان render (که هر تیک با `livePrice` re-render می‌شود) محاسبه شد. تگ حالا زیرِ قیمتِ آخر روی محور می‌نشیند و دنبالِ قیمت می‌آید. fallback به پایین-راست حفظ شد.
- verify: ✅ mount + **تأییدِ زنده**: «45:09» با نقطهٔ سبز دقیقاً زیرِ «LIVE 1.14386» روی محورِ راست. چارت سالم.
- دیپلوی: `0aba3ca` — فقط `frontend-prochart`؛ سایت `200`.
- **وضعیتِ کلی (۱۱ لوپ):** هر ۱۰ بخشِ اصلی حالا تقریباً با TV هم‌ترازند — تولبار (اینتروال+جداکننده؛ ۲-ردیف منتظرِ تصمیمِ مالک)، ریلِ چپ (خوشه+فلای‌اوت)، چارت/گرید، محورِ قیمت (دقت)، لجند، واچ‌لیست، جزئیات، ریلِ راست، منوها/اینتروال، و دیالوگِ اندیکاتور. باقی‌مانده‌ها عمدتاً داده‌محور (حجم) یا نیازمندِ تأییدِ مالک (تولبارِ تک‌ردیف).
- باقی‌مانده: گلیفِ آیکونِ ریلِ چپ (میکرو)؛ دیالوگِ تنظیمات (چرخ‌دنده) تب‌به‌تب؛ آیکونِ ماه/خورشیدِ بازار (audit #19).

## لوپ #12 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۷:۲۵ — لوپِ راستی‌آزمایی (بدونِ تغییر/دیپلوی)
- بررسیِ صادقانهٔ چند موردِ دقیق با کلیک‌تست/اندازه‌گیریِ زنده — همه **از قبل TV-faithful**:
  - دیالوگِ سریعِ چرخ‌دنده (تم/مقیاسِ قیمت عادی/لگاریتمی/درصدی/پایه۱۰۰/کراس‌هیر ۴حالته/توگل‌ها) — سبکِ TV.
  - رنگِ کراس‌هیر = `#9598a1` (رنگِ رسمیِ TV) و نقطه‌چین — دقیق.
  - رنگِ کندل up/down = تمِ روشن `#089981/#f23645`، تیره `#26a69a/#ef5350` — دقیقاً مقادیرِ TV (audit #31).
  - ارتفاعِ دکمه‌های تولبار = ۳۶px (h-9) در محدودهٔ ۳۸–۴۰pxِ TV (audit #16).
  - countdownِ لوپ ۱۱ روی محورِ راست تأیید شد (۳۲:۲۰ زیرِ قیمتِ آخر).
- **تصمیم:** چیزِ واقعی‌ای برای بهبود نبود؛ طبقِ اصلِ «چیزِ الکی عوض نکن»، هیچ تغییری ندادم و **بیلد/دیپلوی هم نزدم** (تا `api` بی‌دلیل recreate نشود). فقط همین لاگ.
- **نتیجه‌گیری:** به **معیارِ توقفِ سند** رسیده‌ایم — هر ۱۰ بخشِ اصلی تقریباً با TV هم‌ترازند. باقی‌ماندهٔ واقعی فقط: (الف) تولبارِ تک‌ردیف (نیازمندِ تأییدِ مالک چون UX/برچسب‌های فارسی را عوض می‌کند)، (ب) مواردِ داده‌محور (حجم/اسپردِ فارکس — data-layer، خارج از اسکوپِ frontend). پیشنهاد به مالک: یا «توقف» + گزارشِ نهایی، یا تصمیم دربارهٔ تولبار.

## لوپ #13 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۷:۴۰
- بخشِ بررسی‌شده (اول): **ریلِ آیکونِ سمتِ راست** — TV-faithful بود (آیکون‌های غیرفعال `TH.text=#5d606b` خاکستریِ خنثی نه آبی؛ اکتیو `accentTint`). تفاوت‌ها فقط انتخابِ آیکون (ستاره↔بوکمارک) که باگ نیست — دست نزدم.
- سپس **تهِ سندِ audit را خواندم** (مورد ۷۶–۱۵۳) تا مطمئن شوم بی‌جهت «تمام» اعلام نکنم؛ موردِ واقعیِ اجرانشده پیدا شد: **#116 گروه‌بندیِ منوی اینتروال**.
- بخشِ اصلاح‌شده: **dropdownِ اینتروال** (لوپ ۶ لیستِ تخت بود) → گروه‌بندیِ سبکِ TV: **دقیقه‌ای / ساعتی / روزانه** با سرگروه‌های خاکستریِ uppercase. هیچ TFای حذف نشد؛ ستاره/منتخب/اکتیو حفظ شد.
- verify: ✅ mount + **کلیک‌تستِ زنده**: منو باز، سه گروه با سرتیتر، همهٔ ۱۰ TF، ۱H اکتیو هایلایت، ستاره‌ها درست.
- دیپلوی: `19dc791` — فقط `frontend-prochart`؛ سایت `200`.
- درسِ صادقانه: قبل از اعلامِ «تمام»، تهِ سندِ audit را کامل بخوان — هنوز مواردِ واقعیِ اجرانشده هست (context menus #119، Object tree #123، بخش‌هایی از دیالوگِ Settings #101–107). این‌ها بزرگ‌ترند؛ در لوپ‌های بعد با احتیاط.

## لوپ #14 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۷:۵۵ — راستی‌آزماییِ سطوحِ بزرگ (بدونِ تغییر/دیپلوی)
- با کلیک‌تستِ زنده سه سطحِ بزرگ که audit با 🔴 علامت‌شان زده بود بررسی شد — **همه از قبل ساخته و TV-faithful‌اند** (سندِ audit کهنه است):
  - **منوی راست‌کلیکِ چارت #۱۱۹** (کد ۱۸۵۸): افزودنِ اندیکاتور/آلارم در قیمت/لانگ/شورت/تریدِ واقعی/پاک‌کردن/پنجرهٔ داده/بازنشانیِ مقیاس/تنظیمات/تمام‌صفحه — کامل.
  - **دیالوگِ کاملِ Settings #۱۰۱–۱۰۷** (`ChartSettingsDialog`): تب‌های Symbol/Status line/Scales/Appearance/Events (سبکِ TV)؛ تبِ Symbol با رنگِ بدنه/حاشیه/فتیله + Hollow/Thin؛ فوترِ Template/Reset/Cancel/OK. کامل.
  - **Object Tree #۱۲۳**: state `drawList`/`showTree` موجود است.
- **تصمیم:** هیچ گپِ واقعی‌ای نبود؛ بدونِ build/deploy، فقط لاگ.
- **جمع‌بندیِ مهم (۱۴ لوپ):** پس از راستی‌آزماییِ زندهٔ همهٔ سطوحِ اصلی، Pro-Chart واقعاً به هم‌ترازیِ کاربردی/ظاهریِ TV رسیده است. سندِ `TV-PARITY-AUDIT.md` قدیمی است و بسیاری از 🔴هایش عملاً پیاده شده‌اند. **کارِ واقعیِ باقی‌مانده فقط:** (۱) تولبارِ تک‌ردیف (نیازمندِ تصمیمِ مالک — UX/برچسب)، (۲) مواردِ داده‌محور (حجم/اسپرد/۰.۰۰٪ فارکس — data-layer، خارج از اسکوپ). **پیشنهادِ صادقانه به مالک: «توقف» + گزارشِ نهایی، یا تصمیم دربارهٔ تولبار.** لوپ به‌عنوانِ دیده‌بانِ regression فعال می‌ماند.

## لوپ #15 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۸:۱۰ — راستی‌آزمایی (بدونِ تغییر/دیپلوی)
- git pull: کامیتِ جدیدی از پروسهٔ دیگر نبود (بدونِ رگرسیون).
- بخشِ بررسی‌شده: **مدالِ جستجوی نماد (Ctrl+K)** — با کلیک‌تستِ زنده. **از قبل TV-faithful:** فیلترهای نوع (همه/فارکس/کریپتو/شاخص‌ها/فلزات/انرژی — #۱۰۸)، ردیف‌ها با آیکون+نماد+توضیحِ فارسی+بَجِ بازار+نقطهٔ رنگی+پرچم+ستاره (#۱۰۹)، چیپ‌های «اخیر»، و ناوبریِ کاملِ کیبورد (پیمایش/انتخاب/بستن — #۱۱۲). فوتر «۶۲ نماد».
- تصمیم: گپِ واقعی‌ای نبود؛ بدونِ build/deploy، فقط لاگ.
- وضعیت بدون تغییر نسبت به لوپ ۱۴: پاریتی حاصل شده؛ منتظرِ «توقف» یا تصمیمِ مالک دربارهٔ تولبار. (کادنسِ ۱۰ دقیقه طبقِ دستورِ صریحِ مالک حفظ شد.)

## لوپ #16 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۸:۲۵ — راستی‌آزمایی (بدونِ تغییر/دیپلوی)
- git pull: بدونِ کامیتِ جدید/رگرسیون.
- بخشِ بررسی‌شده: **منوی نوعِ چارت #۱۱۳–۱۱۵** (کلیک‌تستِ زنده). **از قبل TV-faithful:** ~۱۶ نوع (کندل/توخالی/هایکین/میله/خطی/ناحیه/پایه/پلکانی/رنکو/بازه‌ای/شکستِ خط/کاگی/P&F/کندلِ حجمی/خطی با نشانگر/HLC) هرکدام با **آیکونِ گرافیکی** (#۱۱۴) + نامِ فارسی+انگلیسی + فعالِ هایلایتِ آبی (#۱۱۵).
- تصمیم: گپِ واقعی نبود؛ بدونِ build/deploy، فقط لاگ.
- تعدادِ سطوحِ راستی‌آزمایی‌شده تا کنون ~۱۸؛ همه TV-faithful. پاریتی پابرجا. منتظرِ «توقف»/تصمیمِ تولبار.

## لوپ #17 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۹:۰۰ — دیده‌بان (بدونِ تغییر/دیپلوی)
- git pull: بدونِ کامیتِ جدید/رگرسیون.
- بخشِ بررسی‌شده: **پنلِ سازندهٔ آلارم #۱۳۰–۱۳۶** (کلیک‌تستِ زنده). **از قبل TV-faithful:** شرط‌ساز (قیمتِ نماد/میانه/تقاطعِ صعودی از/مقدار — #۱۳۱)، چندشرطیِ «و...» (#۱۳۲)، فرکانسِ «هربار با کول‌داون» + انقضا (#۱۳۳)، بخشِ «پیام و تحویل» (کانال‌ها — #۱۳۴)، تبِ «آلارم‌ها/لاگ» (#۱۳۶)، دکمهٔ ساختِ آلارم.
- تصمیم: گپِ واقعی نبود؛ بدونِ build/deploy، فقط لاگ.
- **نتیجه:** با بررسیِ آلارم، عملاً همهٔ سطوحِ بزرگِ TV راستی‌آزمایی شدند و TV-faithful‌اند. کارِ پرارزشِ frontend تمام است. کادنسِ دیده‌بانی ۳۰ دقیقه.

## لوپ #18 — ۱۴۰۵/۰۴/۱۹ (2026-07-10) ~۰۹:۳۰ — دیده‌بان (بدونِ تغییر/دیپلوی)
- git pull: بدونِ کامیتِ جدید/رگرسیون. چکِ سلامتِ زنده: سایت `200`، `#root` پر (rootLen ~99.6k)، بدونِ pageerror.
- بخشِ بررسی‌شده: **داکِ پایین #۹۱–۹۳** (کد). موجود است: `BottomDock` با تب‌های «تستِ استراتژی» (StrategyTesterTab)، «یادداشت‌ها» (NotesTab)، بک‌تست، و نمااسکریپت (Pine).
- تصمیم: گپِ واقعی نبود؛ بدونِ build/deploy.
- **جمع‌بندی:** عملاً هر موردِ audit که راستی‌آزمایی شد از قبل ساخته شده. کارِ پرارزشِ frontend کامل است. تنها باقی‌مانده: تولبارِ تک‌ردیف (مالک‌محور) + مواردِ داده‌محور (خارج از اسکوپ). دیده‌بان هر ۳۰ دقیقه، عمدتاً برای گرفتنِ رگرسیونِ کامیت‌های پروسهٔ دیگر.

## لوپ #19 — ۱۴۰۵/۰۴/۱۹ ~۱۰:۰۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سلامتِ زنده: سایت `200`، `#root` پر (~99.6k)، بدونِ pageerror. رگرسیونی نبود، تغییری لازم نبود.

## لوپ #20 — ۱۴۰۵/۰۴/۱۹ ~۱۰:۳۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم (rootLen ~99.6k، بدونِ pageerror). بدونِ تغییر.

## لوپ #21 — ۱۴۰۵/۰۴/۱۹ ~۱۱:۱۰ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #22 — ۱۴۰۵/۰۴/۱۹ ~۱۱:۴۰ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر. (مالک دربارهٔ تعدادِ اجراها پرسید؛ توضیح داده شد که کارِ مفید تا ~لوپ ۱۳ تمام شد و بقیه دیده‌بان‌اند — پیشنهادِ توقف داده شد، منتظرِ تأیید.)

## باگ‌فیکسِ گزارشِ مالک — ۱۴۰۵/۰۴/۱۹ ~۱۱:۵۵ — سطلِ تکراریِ ریلِ چپ
- گزارشِ مالک: در پایینِ ریلِ چپ **دو سطلِ آشغال** بود.
- ریشه‌یابی با probeِ زندهٔ DOM: دو `lucide-trash2` تأیید شد — (۱) «حذفِ همهٔ ترسیم‌ها»ی خوشهٔ ToolRail (لوپ ۳) و (۲) دکمهٔ قدیمیِ «پاکِ همهٔ ترسیم‌ها»ی جامانده در انتهای ریل (BazaarNama خطِ ۱۶۸۲). **هر دو دقیقاً `clearAll()` را صدا می‌زدند.**
- رفع: دکمهٔ دومِ تکراری حذف شد؛ قابلیت دست‌نخورده ماند (trashِ ToolRail باقی است). «پاکِ آخرین ترسیم» (Minus) که قابلیتِ متمایزی است نگه داشته شد.
- verify: ✅ mount لوکال + **probeِ زنده: تعدادِ trash از ۲ به ۱ رسید.** دیپلوی: `cc08e92`، سایت `200`.
- یادداشت برای مالک: هم‌پوشانیِ دیگری هست ولی «تکرارِ عین» نیست و در «ریلِ چپ» نیست: مگنت/پروفایلِ حجم/پنجرهٔ داده هم در ریل و هم در منوی چرخ‌دندهٔ تولبار toggle دارند (دسترسیِ سریع، سبکِ TV). اگر مالک بخواهد این‌ها هم یکی شوند، جداگانه اعمال می‌شود.

## لوپ #23 — ۱۴۰۵/۰۴/۱۹ ~۱۲:۰۵ — اسکنِ تکراری (پیگیریِ گزارشِ مالک)
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم.
- اسکنِ خودکارِ آیکون‌های تکراری (probeِ DOMِ زنده) در تولبارِ بالا و ریلِ راست: تنها «تکرار» = `chevron-down` ×۶ در تولبار که **باگ نیست** (فلشِ استانداردِ هر dropdown). ریلِ راست هیچ تکراری ندارد.
- نتیجه: بجز سطلِ آشغالِ ریلِ چپ (رفع‌شده)، دکمه/آیکونِ تکراریِ دیگری نیست. بدونِ تغییر.

## لوپ #25 — ۱۴۰۵/۰۴/۱۹ ~۱۲:۲۰ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #26 — ۱۴۰۵/۰۴/۱۹ ~۱۲:۳۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #27 — ۱۴۰۵/۰۴/۱۹ ~۱۲:۴۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #28 — ۱۴۰۵/۰۴/۱۹ ~۱۲:۵۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #29 — ۱۴۰۵/۰۴/۱۹ ~۱۳:۱۰ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #30 — ۱۴۰۵/۰۴/۱۹ ~۱۳:۲۰ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #31 — ۱۴۰۵/۰۴/۱۹ ~۱۳:۳۰ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## باگ‌فیکسِ گزارشِ مالک — ۱۴۰۵/۰۴/۱۹ ~۱۳:۴۰ — تمیزسازیِ ازدحامِ ریلِ چپ
- گزارشِ مالک: پایینِ ریلِ چپ هنوز «بهم‌ریخته» است. تأیید شد: مربعِ آبیِ توپرِ رنگ + VP + undo/redo + درختِ آبجکت + پنجرهٔ داده + منها (پاکِ آخرین) — ۷ آیکونِ اضافه که در ریلِ TV نیستند.
- رفع: ریلِ چپ حالا فقط **ابزارهای ترسیم + خوشهٔ مگنت/قلم/قفل/چشم/سطل + رنگِ ترسیم** است (دقیقاً سبکِ TV). undo/redo/درختِ آبجکت به **تولبارِ بالا** منتقل شدند (جای TV، audit #8)؛ VP و پنجرهٔ داده در منوی چرخ‌دنده + کلیک‌راست باقی‌اند (بدونِ حذفِ قابلیت)؛ «پاکِ آخرین» که با Undo تکراری بود حذف شد.
- verify: ✅ mount + **تأییدِ زنده**: ریلِ چپ تمیز شد، undo/redo در تولبار دیده می‌شوند. دیپلوی `90488d9`، سایت `200`.

## لوپ #32 — ۱۴۰۵/۰۴/۱۹ ~۱۳:۴۵ — دیده‌بان (تأییدِ رفعِ ریل)
- سلامتِ زنده: سایت `200`، بدونِ خطا؛ probe تأیید کرد ریلِ چپ حالا فقط **۱ سطل** دارد و تمیز است. منتظرِ تصمیمِ مالک دربارهٔ **تولبارِ تک‌ردیف** (بزرگ‌ترین تفاوتِ باقی‌مانده با TV) یا «توقف».

## ⭐ تولبارِ تک‌ردیف — ۱۴۰۵/۰۴/۱۹ ~۱۴:۱۵ (با تأییدِ صریحِ مالک)
- مالک با AskUserQuestion گزینهٔ «تولبار را تک‌ردیف کن» را انتخاب کرد.
- کار (۲ گام، ۲ دیپلوی): **آیکون‌محورکردنِ دکمه‌های ثانویه** (نمااسکریپت/هشدار/بازپخش/سیگنال AI/نوعِ چارت/سشن‌ها/چیدمان/اندیکاتورها → آیکون + `title` tooltip، بدونِ حذفِ قابلیت) + کاهشِ `gap-1.5→gap-1`.
- نتیجهٔ اندازه‌گیری: عرضِ فرزندانِ تولبار **۲۶۵۱px → ۱۳۰۰px**؛ ردیفِ دوم حذف شد.
- verify: ✅ mount + **تأییدِ زنده**: اسکرین‌شات نشان می‌دهد تولبار **تک‌ردیف** است (rowTops=[10,20])؛ کلیک‌تست: آیکونِ اندیکاتورها هنوز دیالوگ را باز می‌کند؛ بدونِ pageerror.
- دیپلوی: `d6ded94` + `1f000f3`، سایت `200`.
- **این بزرگ‌ترین تغییرِ محسوس به‌سمتِ TV بود** (بازخوردِ مالک: «شبیهِ TV نشده»). حالا تولبار مثلِ TV تک‌ردیفِ آیکون‌محور است.

## لوپ #36 — ۱۴۰۵/۰۴/۱۹ ~۱۴:۴۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #37 — ۱۴۰۵/۰۴/۱۹ ~۱۴:۵۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #38 — ۱۴۰۵/۰۴/۱۹ ~۱۵:۰۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #39 — ۱۴۰۵/۰۴/۱۹ ~۱۵:۱۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #40 — ۱۴۰۵/۰۴/۱۹ ~۱۵:۲۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #41 — ۱۴۰۵/۰۴/۱۹ ~۱۵:۳۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #42 — ۱۴۰۵/۰۴/۱۹ ~۱۵:۴۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #43 — ۱۴۰۵/۰۴/۱۹ ~۱۵:۵۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر.

## لوپ #44 — ۱۴۰۵/۰۴/۱۹ ~۱۶:۰۵ — دیده‌بان
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم. بدونِ تغییر. (رکوردِ لوپ‌به‌لوپ در حافظهٔ دائمی هم ذخیره شد — `prochart-tv-parity`.)

## لوپ #35 — ۱۴۰۵/۰۴/۱۹ ~۱۴:۳۵ — دیده‌بان (نمای کلی)
- سلامت: سایت `200`، بدونِ خطا. اسکرین‌شاتِ کامل: نمای کلی حالا **واضحاً TV-مانند** است — تولبارِ تک‌ردیفِ آیکونی + ریلِ چپِ تمیز + واچ‌لیستِ کامل + محورِ دقیق + countdown روی محور. بدونِ تغییر (فقط تأیید).

## لوپ #34 — ۱۴۰۵/۰۴/۱۹ ~۱۴:۲۵ — دیده‌بان (تأییدِ تولبار)
- سلامت: سایت `200`؛ اندازه‌گیری تأیید کرد تولبار همچنان **تک‌ردیف** است (بدونِ ردیفِ دوم). بدونِ رگرسیون/تغییر.

## لوپ #33 — ۱۴۰۵/۰۴/۱۹ ~۱۴:۰۰ — دیده‌بان + بازاندازه‌گیریِ تولبار
- سلامت: سایت `200` و سالم. اندازه‌گیریِ تولبار = **۲۶۵۱px** (۳۰ فرزند). یافته: صرفِ آیکون‌محورکردنِ برچسب‌ها ~۶۰۰px صرفه‌جویی می‌کند → ~۲۰۵۰px، هنوز > ۱۴۴۰؛ **تک‌ردیفِ واقعی نیازمندِ جمع‌کردنِ ~نیمی از کنترل‌ها در منوی overflow است** (refactorِ بزرگ). آیکون‌محورکردنِ نصفه هم برچسب‌ها را می‌برد هم تک‌ردیف نمی‌شود — بدونِ تصمیمِ مالک انجام نشد. بدونِ تغییر.

## لوپ #24 — ۱۴۰۵/۰۴/۱۹ ~۱۲:۱۰ — اسکنِ تکراریِ پنلِ راست
- git pull بدونِ کامیتِ جدید؛ سایت `200` و سالم.
- اسکنِ پنلِ راست (واچ‌لیست/جزئیات): `flag`×۵ و `x`×۵ (یکی per ردیفِ واچ‌لیست — پرچم+حذف، per-item نه تکرار)، `chevron-down`×۴ (کشو+سرگروه‌ها)، `info`×۲ (دو بخشِ متفاوت). همه **مشروع، نه باگ**.
- نتیجه: اسکنِ تکراری در همهٔ نواحی کامل شد؛ **تنها باگِ تکراری همان سطلِ آشغال بود که رفع شد.** بدونِ تغییر.

> نکتهٔ عملیاتی: `docker compose ... up -d --build frontend-prochart` (فرمانِ مصوبِ SERVER-HANDOFF) به‌عنوانِ وابستگی، `api` را هم recreate می‌کند (سریع، healthy برمی‌گردد). timescaledb/redis فقط health-wait می‌شوند، recreate نه.
> برنچ مشترک است: پروسهٔ دیگری هم کامیتِ کریپتو (`5a696d7`, `066dfb6`) اضافه کرده؛ هر لوپ `git pull` لازم است.
> محدودیتِ verify: صفحهٔ لوکال به‌خاطرِ نبودِ APIِ same-origin به ارور-باندریِ آفلاین می‌رود، پس فقط mount/TDZ چک می‌شود؛ صحتِ تعاملیِ چارت را با اسکرین‌شاتِ **زندهٔ** (و در صورتِ نیاز کلیک/hoverتستِ playwright) بعد از دیپلوی تأیید می‌کنم — این لوپ نشان داد اسکرین‌شاتِ زنده حیاتی است (تلاشِ اول ظاهراً سبز ولی عملاً کار نکرد).

## لوپ #45 — ۱۴۰۵/۰۴/۱۹ — parity: ساعتِ زندهٔ نوارِ پایین
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زندهٔ ۱۴۴۰px گرفته شد + کلیپِ نوارِ پایین.
- **یافته (مقایسه با TradingView):** نوارِ پایینِ چارت فقط تب‌های بازهٔ سریع (`1D…All`) را چپ‌چین داشت و **سمتِ راستش خالی بود**؛ در TradingView سمتِ راستِ همین نوار **ساعتِ زندهٔ منطقهٔ زمانیِ چارت** (HH:MM:SS) نمایش داده می‌شود.
- **پیاده‌سازی (green apple، اصیل):** state `tzClock` + افکتِ `setInterval` هر ثانیه با `Intl.DateTimeFormat` روی `tz`ِ فعال؛ رندرِ راست‌چینِ `HH:MM:SS (لیبلِ منطقه)` در نوارِ پایین با `ml-auto`. از زیرساختِ موجودِ `tz`/`TIMEZONES` استفاده شد؛ تاگل‌های مقیاس در تولبار دست‌نخورده (بدونِ کلاتر/تکرار).
- قوانین: بدونِ صرافی/بروکر؛ آیکون/لوگوی اختصاصی؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: `npm run build` سبز (۱۷۷۶ ماژول، بدونِ خطا). **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنسِ ~۱۰-تغییری (۱/۱۰ در انتظار).**

## لوپ #46 — ۱۴۰۵/۰۴/۱۹ — parity: آیکونِ ساعت + بررسیِ drift و برنچ
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ لجند (۱x و ۳x).
- **بررسیِ drift (لجندِ OHLC):** ابتدا به‌نظر رسید مقادیرِ O/H/L/C روی سایتِ زنده بی‌رنگ‌اند (احتمالِ عقب‌بودنِ دیپلوی)، اما **کلیپِ ۳x نشان داد مقادیر واقعاً قرمز/سبزِ جهت‌دارند** — سایتِ زنده با کدِ ریپو **مطابق** است. خطای خواندنِ متنِ ریزِ ۱x بود. **هیچ drift‌ای در OHLC نیست.**
- **یافتهٔ زیرساختی (مهم):** ۱) هیچ workflowِ auto-deployِ وب روی این برنچ نیست (فقط `android-apk.yml` روی برنچِ `claude/android-apk-project-build-l9a960`). ۲) برنچِ جاری `claude/pro-chart-tradingview-parity-ewbpoy` نسبت به برنچِ اندروید **۹۱ جلو / ۱۵۳ عقب** است (دو مسیرِ واگرا). لاگ‌های لوپِ قبلی هم روی همین برنچِ parity هستند ⇒ **این برنچ، برنچِ کارِ parity است** و ظاهراً منبعِ بیلدِ وب (چون OHLC-coloring زنده هست).
  - **پرسشِ باز برای مالک:** منبعِ دقیقِ بیلدِ پروداکشنِ `pro-chart.com` و روالِ دیپلویِ وب (دستی docker compose روی همین سرور؟) — تا دیپلویِ عمدی و بی‌ریسک باشد.
- **پیاده‌سازی (green apple):** به ساعتِ نوارِ پایینِ #45 یک **آیکونِ ساعتِ اختصاصی (lucide Clock, 12px)** قبل از زمان اضافه شد — مطابقِ چیدمانِ نوارِ پایینِ TradingView. ایزوله و کم‌ریسک؛ با #45 یک‌جا دیپلوی می‌شود.
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ اختصاصی نه کپیِ ترِیدمارک؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز (بدونِ خطا). **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنسِ ~۱۰-تغییری (۲/۱۰ در انتظار).**

## لوپ #47 — ۱۴۰۵/۰۴/۱۹ — parity: وضعیتِ بازار در پنلِ جزئیات
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از ریلِ راست.
- **بررسی‌ها (بدونِ تغییرِ لازم):** ریلِ آیکونِ راست (`RightIconRail.jsx`) از قبل کاملِ TV است — تولتیپ، اکتیوِ tinted، پنل‌های watch/alerts/cal/news/screener/ideas. لجندِ OHLC هم درست است.
- **یافته:** در پنلِ **جزئیاتِ نماد** (`panels/Details.jsx`) قیمتِ بزرگ + تغییر + «آخرین به‌روزرسانی · GMT» بود، اما **نشانگرِ وضعیتِ بازار (باز/بسته)** نداشت — در حالی که سرتیترِ نمادِ TradingView «● Market open/closed» را کنارِ قیمت نشان می‌دهد. (ردیف‌های واچ‌لیست از قبل این نقطهٔ سبز/خاکستری را داشتند.)
- **پیاده‌سازی (green apple):** به خطِ «آخرین به‌روزرسانی» یک نشانگرِ **● بازار باز/بسته** اضافه شد؛ **دقیقاً هم‌راستا با heuristicِ موجودِ واچ‌لیست** (midِ زندهٔ معتبر ⇒ سبزِ باز، نبودش ⇒ خاکستریِ بسته) — بدونِ ریاضیِ سشنِ جدید/پرریسک، سازگار بینِ پنل‌ها.
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۳/۱۰ در انتظار).**

## لوپ #48 — ۱۴۰۵/۰۴/۱۹ — parity(باگ‌رفع): وصل‌کردنِ میان‌بُرهای مستند اما بی‌عمل
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از ریلِ چپ و تولبارِ بالا.
- **بررسی‌ها (کاملِ TV، بدونِ تغییر):** ریلِ ابزارِ چپ (گروه‌بندی + کارتِ زیرمنو + مگنت/قفل/چشم/سطل)، تولبارِ بالا، و **سیستمِ هات‌کیِ ۶۳-میان‌بُری** + دیالوگِ راهنما همگی از قبل عالی‌اند.
- **باگِ واقعی (parity):** دیالوگِ «راهنمای میان‌بُرها» ~۱۵ میان‌بُر را **نمایش می‌داد اما handler نداشتند** (فشردنشان هیچ کاری نمی‌کرد) — برخلافِ TradingView که همهٔ میان‌بُرهای مستند کار می‌کنند.
- **رفع (green apple، ایمن):** ۸ handlerِ تأییدشده وصل شد با state/refِ موجود: `invertScale`(Alt+I)، `longshort`(Alt+L)، `settings`(Ctrl+,)، `newAlert`(Alt+A)، `hideAll`(Ctrl+Alt+H)، `zoomIn`(↑/+)، `zoomOut`(↓/−)، `scrollHome`(Home). عمداً `symbolSearch`/`replayPlay`/کلیپ‌بورد (copy/paste/selectAll) را **نزدم** چون به ref/تابعِ نامطمئن نیاز داشتند (ایمنی > کمیت).
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق (بدونِ آیکونِ جدید)؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۴/۱۰ در انتظار).**

## لوپ #49 — ۱۴۰۵/۰۴/۱۹ — parity: مرتب‌سازیِ واچ‌لیست با کلیکِ سرستون
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از واچ‌لیست.
- **بررسی‌ها (کاملِ TV):** واچ‌لیست (لیستِ چنددسته‌ای، پرچم‌های رنگی، نقطهٔ زندهٔ سبز، پیلِ رنگیِ ٪تغییر، سکشن‌ها با شمارنده) از قبل بسیار وفادار است.
- **یافته:** منطقِ مرتب‌سازی (`meta.sortBy` = name/price/changePct + `sortDir`) از قبل در state وجود داشت، اما **سرستون‌ها `span`ِ غیرقابلِ‌کلیک بودند** — در حالی که در TradingView کلیکِ سرستون واچ‌لیست را مرتب می‌کند (با فلشِ ▲/▼).
- **پیاده‌سازی (green apple، ایمن):** سرستون‌های «نماد/آخرین/تغییر٪» به دکمهٔ قابلِ‌کلیک تبدیل شدند؛ سه‌حالته **manual → صعودی → نزولی → manual** (برگشت به ترتیبِ دستیِ کاربر)، فلشِ ▲/▼ و رنگِ accent روی ستونِ فعال. **از همان زیرساختِ موجودِ sort استفاده شد** (بدونِ منطقِ جدید)؛ مرتب‌سازی درونِ هر سکشن اعمال می‌شود. هر دو نمای list و table پوشش داده شد.
- قوانین: بدونِ صرافی/بروکر؛ فقط تعامل (بدونِ آیکونِ جدید)؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۵/۱۰ در انتظار).**

## لوپ #50 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: گلیفِ شکستهٔ چیپِ شمارشِ‌معکوس
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از محورِ قیمت/چیپِ شمارش.
- **یافته (باگِ بصریِ واقعی):** چیپِ «شمارشِ معکوس تا بسته‌شدنِ کندل» روی محور، پیش از زمان یک **ایموجیِ ساعتِ ⏱** داشت که فونتِ سایت آن را ندارد ⇒ به‌صورتِ مربعِ خالی (□/tofu) رِندر می‌شد. در TradingView این چیپ یک آیکونِ ساعتِ تمیز دارد.
- **رفع (green apple، اصیل):** ایموجی با **آیکونِ ساعتِ اختصاصیِ SVG** (دایره + عقربه، ۱۱px، `currentColor`) جایگزین شد — بدونِ وابستگی به فونت، هم‌رنگِ چیپ. grep تأیید کرد نمونهٔ دیگری از این ایموجی‌به‌جای‌آیکون در فایل‌های اصلیِ چارت نیست.
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۶/۱۰ در انتظار).**

## لوپ #51 — ۱۴۰۵/۰۴/۱۹ — 🚀 دیپلویِ کارِ انباشته (۴۵–۵۰) + تأییدِ زنده
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`.
- **بررسیِ تعاملی:** مدالِ جستجوی نماد (Ctrl+K) را با Playwright باز کردم و اسکرین‌شات گرفتم — **کاملِ TradingView**: تب‌های دسته (همه/فارکس/کریپتو/فلزات/شاخص/انرژی — بدونِ صرافی)، چیپ‌های اخیر، ردیف‌های غنی (پرچم/بج/ستاره)، و راهنمای کیبورد در فوتر (↑↓ پیمایش · ↵ انتخاب · Esc). **بدونِ نیاز به تغییر.**
- **حلِ گلوگاهِ دیپلوی:** از `docker-compose.prochart.yml` تأیید شد فرانتِ پروداکشن از **همین working-tree/branch** بیلد می‌شود (`build.context: ./frontend/prochart`) — یعنی منبعِ وبِ پروداکشن همین برنچ است (سازگار با تطابقِ OHLCِ لوپ #46). پرسشِ بازِ لوپ‌های ۴۶–۵۰ پاسخ گرفت.
- **دیپلوی (مصوبِ مالک — کادنس):** `docker compose -f docker-compose.prochart.yml up -d --build frontend-prochart` روی HEAD `531bc16`. api طبقِ انتظار recreate و سریع healthy شد؛ frontend healthy، سایت `200`.
- **تأییدِ زندهٔ پس از دیپلوی (اسکرین‌شات):** ۱) چیپِ شمارشِ معکوس حالا **آیکونِ ساعتِ تمیز** دارد (□ رفع شد — #50). ۲) نوارِ پایین حالا **🕐 ۲۲:۰۹:۱۸ (تهران)** را نشان می‌دهد (#45/#46). هر دو زنده و درست.
- نتیجه: **۶ تغییرِ لوپ‌های ۴۵–۵۰ اکنون لایو و تأییدشده‌اند؛ شمارندهٔ در-انتظار → ۰/۱۰.**

## لوپ #52 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: بازهٔ روز باید قیمتِ زنده را دربر بگیرد
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از پنلِ جزئیات.
- **تأییدِ دیپلویِ #51:** «● بازار باز» در پنلِ جزئیات (#47) حالا زنده است. ✅
- **یافته (باگِ واقعی، در اسکرین‌شاتِ زنده دیده شد):** «بازهٔ روز» کف/سقف را فقط از **کندلِ روزانه** می‌گرفت و قیمتِ زندهٔ `ref` را fold نمی‌کرد. وقتی قیمتِ فعلی (۱٫۱۴۱۳۶) از کفِ نمایش‌داده‌شده (۱٫۱۴۲۱۳) پایین‌تر رفت، برچسب‌ها کهنه ماندند و knob در لبهٔ چپ پین شد — برخلافِ TradingView که بازهٔ روز همیشه قیمتِ فعلی را دربر می‌گیرد. (بازهٔ ۵۲ هفته از قبل این folding را داشت — ناسازگاری.)
- **رفع (green apple، ایمن):** `lo = min(day.l, ref)` و `hi = max(day.h, ref)` — دقیقاً هم‌الگو با folding موجودِ بازهٔ ۵۲ هفته. حالا برچسب‌ها، knob و درصدِ موقعیت همه قیمتِ زنده را دربر می‌گیرند.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۱/۱۰ در انتظار).**

## لوپ #53 — ۱۴۰۵/۰۴/۱۹ — parity: «کپیِ قیمت» در منوی راست‌کلیک
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از مدال‌ها/منو.
- **بررسی‌های تعاملی (کاملِ TV، بدونِ تغییر):** دیالوگِ اندیکاتورها (Personal/Built-in/Community، ۹۱ تکنیکال، ۴ تب) و منوی راست‌کلیکِ چارت (افزودنِ اندیکاتور/آلارم، لانگ/شورت رو‌به‌جلو، ترید، پاک‌سازی، پنجرهٔ داده، بازنشانیِ مقیاس، تنظیمات، تمام‌صفحه) هر دو بسیار وفادارند.
- **یافته:** منوی راست‌کلیک، برخلافِ TradingView، آیتمِ **«کپیِ قیمتِ نقطهٔ کلیک»** را نداشت (TV زیرِ راست‌کلیک Copy price دارد).
- **پیاده‌سازی (green apple، ایمن):** آیتمِ «کپیِ قیمت: {قیمتِ نقطهٔ کلیک}» با آیکونِ **SVGِ اختصاصیِ کلیپ‌بورد** (بدونِ importِ جدید) افزوده شد؛ `navigator.clipboard.writeText` با try/catch. درست کنارِ آیتمِ آلارم (هم‌گروهِ اکشن‌های مبتنی‌بر‌قیمت) قرار گرفت.
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۲/۱۰ در انتظار).**

## لوپ #54 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: اعشارِ اشتباهِ شاخص‌ها در اسکنر
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از دیالوگِ تنظیمات و پنلِ اسکنر.
- **تأییدِ دیپلوی:** دیالوگِ تنظیماتِ چارت با **Ctrl+,** باز شد (وصلِ #48 زنده است ✅) — تب‌های نماد/خطِ وضعیت/مقیاس/ظاهر/رویدادها، رنگِ پیش‌فرضِ کندلِ #EF5350/#26A69A (عینِ TV)، تمپلیت + بازنشانی.
- **یافته (باگِ واقعی، در پنلِ اسکنر دیده شد):** `JP225` با **۵ رقمِ اعشار** نمایش داده می‌شد (`69214.70000`) در حالی که بقیهٔ شاخص‌ها ۲ رقمی‌اند. علت: تابعِ `digits()` در `Screener.jsx` فقط `US30|US500|NAS100|DE40|US100` را می‌شناخت و `JP225/SPX/DJI/NDX/UK100/GER40/…` را جا انداخته بود ⇒ سقوط به پیش‌فرضِ ۵. (نقشهٔ اصلیِ `priceDigits` صفحهٔ چارت درست بود — ناسازگاریِ محلیِ اسکنر.)
- **رفع (green apple، ایمن):** رجکسِ شاخص‌ها با **INDEX setِ کاملِ `symbolMeta`** هم‌راستا شد؛ نقره (`XAG`) → ۳ رقم و `UKOIL` هم اضافه شد. حالا شاخص‌ها همه‌جا ۲ رقمی‌اند (مثلِ TradingView).
- قوانین: بدونِ صرافی/بروکر؛ فقط فرمت‌بندی؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۳/۱۰ در انتظار).**

## لوپ #55 — ۱۴۰۵/۰۴/۱۹ — parity: افوردنسِ کلیک‌پذیریِ کارت‌های خبر
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ ۳x از پنلِ اخبار.
- **بررسی‌ها (کاملِ TV، بدونِ تغییر):** پنلِ اخبار بسیار وفادار است — منبع/زمانِ نسبیِ زندهٔ `relTime` (با «هم‌اکنون» زیرِ ۶۰ث و تیکِ ۳۰ثانیه‌ای)، نوارِ اهمیت، برچسبِ «جدید/مهم»، نقطهٔ رنگیِ impact، تگِ نمادها، و کلیک روی کارت → بازکردنِ `a.url` در تبِ جدید (`noopener`).
- **بررسیِ ثباتِ اعشار (پیروِ #54):** هر ۴ هِلپرِ اعشار (symbolMeta/BazaarNama/Screener/Details) حالا شاخص‌ها را ۲ رقمی می‌دهند — **باگِ cross-surface باقی نماند.**
- **یافته:** کارت‌های خبر حتی وقتی `a.url` نداشتند هم افوردنسِ کلیک (hover + pointer) نشان می‌دادند و هیچ نشانگرِ «بازشدن در منبع» نبود — برخلافِ آیتم‌های خبریِ TradingView.
- **پیاده‌سازی (green apple، ایمن):** cursor/hover حالا مشروط به وجودِ `a.url` است؛ `title`/`aria-label` افزوده شد؛ و یک **آیکونِ external-linkِ اختصاصیِ SVG** فقط هنگامِ hover و فقط وقتی لینک هست نمایان می‌شود (دِکلاترد).
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۴/۱۰ در انتظار).**

## لوپ #56 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگِ موبایل: گلیفِ شکستهٔ راهنمای ژست
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + **اسکرین‌شاتِ موبایل (۳۹۰px)** پس از رد کردنِ آنبوردینگ.
- **بررسی‌ها:** ۱) پنلِ تقویمِ اقتصادی کاملِ TV است (فیلترِ اهمیت بالا/متوسط/پایین، منطقهٔ زمانی، «مرتبط با EURUSD»، گروه‌بندیِ روزانه با «امروز»، ستون‌های واقعی/پیش‌بینی/قبلی + آیکونِ میله‌ایِ اهمیت). ۲) **لایوتِ موبایل** بسیار وفادار است (تاپ‌بار، SELL/BUY+اسپرد، نوارِ ناوبریِ پایین پروفایل/بازارها/AI/واچ‌لیست/چارت). تأیید شد که رفعِ #50 (آیکونِ ساعتِ countdown) و #45/#46 (ساعتِ نوارِ پایین) روی موبایل هم زنده‌اند.
- **یافته (باگِ واقعیِ موبایل):** راهنمای ژستِ بارِاولِ موبایل («برای جزئیات نگه‌دار · با دو انگشت زوم کن») با ایموجیِ **👆** شروع می‌شد که فونتِ سایت آن را ندارد ⇒ **□ (tofu)**. چون فقط در حالتِ compact + بارِ اول است، اسکن‌های دسکتاپ آن را ندیده بودند.
- **رفع (green apple، اصیل):** ایموجی با **آیکونِ ژستِ لمس/اشارهٔ اختصاصیِ SVG** (۱۴px، `currentColor`) جایگزین شد. grep تأیید کرد گلیفِ ایموجیِ دیگری در chromeِ چارت نمانده. `✕`ِ بستن سالم بود.
- **نکته برای مالک (نه تغییرِ parity):** متنِ آنبوردینگِ موبایل صریحاً نامِ «TradingView» را می‌برد؛ ذکر شد تا در صورتِ تمایل خودت تصمیم بگیری (تصمیمِ محصولی/حقوقی، نه گپِ فنی).
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه (دسکتاپ+موبایل) انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۵/۱۰ در انتظار).**

## لوپ #57 — ۱۴۰۵/۰۴/۱۹ — 🚀 دیپلویِ کارِ انباشته (۵۲–۵۶) + تأییدِ زنده
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`.
- **بررسی‌های تعاملی (بدونِ تغییرِ لازم):** ۱) پنلِ «سیگنال AI» (ویژگیِ اختصاصیِ Pro-Chart، نه TV) — گلیفِ ✨ در کلیپِ ۳x تمیز رِندر شد (tofu نبود). ۲) صفحهٔ موبایلِ «بازارها و اخبار» و لایوتِ موبایل وفادارند. حینِ بررسی تأیید شد که باگِ tofuِ کوچ‌مارک (#56) **هنوز روی سایتِ زنده بود** (چون از #51 دیپلوی نشده بود) — یعنی رفع لازم بود.
- **دیپلوی (کادنس، ~۵ تغییر انباشته که ۳تاش باگِ زندهٔ کاربر-محور بود):** `docker compose ... up -d --build frontend-prochart` روی HEAD `d19dcb1`. api recreate و سریع healthy؛ frontend healthy، سایت `200`.
- **تأییدِ زندهٔ پس از دیپلوی (اسکرین‌شاتِ موبایلِ ۳x):** کوچ‌مارکِ ژست حالا **آیکونِ دستِ لمسِ تمیز** دارد (□ رفع شد — #56). ✕ سالم. سایرِ رفع‌ها (اعشارِ اسکنر #54، بازهٔ روزِ زنده #52، افوردنسِ خبر #55، کپیِ قیمت #53) هم در همین بیلد لایو شدند.
- نتیجه: **۵ تغییرِ لوپ‌های ۵۲–۵۶ اکنون لایو و تأییدشده؛ در-انتظار → ۰/۱۰.**

## لوپ #58 — ۱۴۰۵/۰۴/۱۹ — parity: آیکونِ متمایز برای نوع‌چارت‌های تخصصی
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسی (نوعِ چارت):** فهرستِ ۱۳ نوع‌چارتِ Pro-Chart (کندل/توخالی/هایکین/میله/خطی/ناحیه/پایه/پلکانی/رنکو/بازه‌ای/شکستِ‌خط/کاگی/P&F) با TradingView هم‌تراز است.
- **یافته (گپِ بصری):** در منوی نوعِ چارت، انواعِ تخصصی آیکونِ متمایز نداشتند — **renko/range/linebreak همه آیکونِ کندل** و **kagi/pnf هر دو آیکونِ خط** بودند؛ از هم قابلِ‌تشخیص نبودند. در TradingView هر نوع آیکونِ یکتا دارد.
- **پیاده‌سازی (green apple، اصیل):** ۵ آیکونِ **SVGِ اختصاصی** ساخته شد (نه کپیِ آیکونِ TV): رنکو=آجرهای پلکانی، بازه‌ای=میله‌های افقی، شکستِ‌خط=بلوک‌های عمودی، کاگی=زیگزاگِ کلفت/نازک، P&F=X و O. هر کدام propِ `size` می‌گیرند تا با اینترفیسِ آیکونِ موجود سازگار باشند؛ در `CHART_TYPES` وصل شدند.
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۱/۱۰ در انتظار).**

## لوپ #59 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ «خطوطِ شبکه» در تنظیمات
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + تبِ «مقیاس و خطوط»ِ دیالوگِ تنظیمات (Ctrl+,).
- **بررسی‌ها (کاملِ TV):** پنلِ آلارم (شرط/مقدار/فرکانسِ تریگر+کول‌داون/انقضا/پیام) و تبِ Scalesِ تنظیمات (حالتِ مقیاس عادی/لاگ/درصد، وارونه، قفلِ ratio، خطِ قیمتِ آخر، شمارشِ معکوس، نمادِ ارز، واحد) هر دو وفادارند.
- **یافتهٔ باگِ واقعی (مهم):** دیالوگِ تنظیمات ~۳۰ کلید دارد ولی `onChange`ِ والد فقط **۵ کلید** (scaleMode/scaleInvert/scaleLock/slVolume/crosshairStyle) را به چارت وصل می‌کرد؛ بقیه (رنگِ کندل، حاشیه/فتیله، توخالی/نازک، **خطوطِ شبکه**، خطِ قیمتِ آخر، countdown/currency/unit، پس‌زمینه…) فقط در `chartSettingsOverrides` ذخیره می‌شدند و **هیچ اثری روی چارت نداشتند** (مثلِ میان‌بُرهای مردهٔ #48).
- **رفع (این لوپ — ایمن و robust):** **خطوطِ شبکه** (`gridHorz/gridVert` + رنگ‌ها) وصل شد؛ chart-level پس با تعویضِ نماد/نوع‌چارت می‌ماند، و افکتِ تم هم اصلاح شد تا با toggleِ تم ریست نشود. بقیهٔ کلیدها برای لوپ‌های بعدی یادداشت شد (رنگِ کندل بعدی‌ترین اولویت است ولی series-level و نیازمندِ persist در بازسازیِ سری‌ست).
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۲/۱۰ در انتظار).**

## لوپ #60 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ «رنگِ کندل» در تنظیمات
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **ادامهٔ رفعِ #59 (کلیدهای مردهٔ تنظیمات):** پرتقاضاترین کلیدِ مرده = **رنگ/حاشیه/فتیلهٔ کندل** (`symUpColor/symDownColor/symBorders*/symWick*`). قبلاً فقط در state ذخیره می‌شد و هیچ اثری روی چارت نداشت.
- **چالشِ فنی:** رنگِ کندل series-level است و افکتِ سری‌ساز (۶۹۱) دیتا را **رفچ** می‌کند؛ پس نمی‌شد overrides را به depهای `buildPriceSeries` افزود (رفچِ دوباره به‌ازای هر کلیکِ رنگ = بد).
- **رفع (green apple، ایمن و بدونِ رفچ):** ۱) هلپرِ `candleOptsFrom(overrides, hollow)` (fallback به تمِ up/down، toggleهای حاشیه/فتیله). ۲) `settingsRef` آینه‌ایِ تنظیمات تا هر ۴ محلِ ساختِ CandlestickSeries (candles/hollow/heikin/renko/range/linebreak) آخرین رنگ‌ها را بخوانند (persist با تعویضِ نماد/نوع‌چارت، بدونِ افزودن به depها). ۳) در `onChange` رنگ‌ها **زنده** با `applyOptions` روی سریِ فعال اعمال می‌شوند (فقط انواعِ کندلی). مسیرِ پیش‌فرض بدونِ تغییر (overridesِ خالی ⇒ همان رنگِ تم) پس **بدونِ رگرسیون**.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۳/۱۰ در انتظار). باقیِ کلیدهای مرده (خطِ قیمتِ آخر، countdown/currency/unit، پس‌زمینه، کراس‌هیرِ رنگی) برای لوپ‌های بعد.**

## لوپ #61 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ «رنگِ پس‌زمینه» در تنظیمات
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **ادامهٔ پاک‌سازیِ کلیدهای مردهٔ تنظیمات (#59 شبکه، #60 کندل):** این لوپ **رنگِ پس‌زمینهٔ چارت** (`bgColor`) وصل شد.
- **رفع (green apple، ایمن):** chart-level `applyOptions({ layout: { background: { color } } })`؛ در `onChange` زنده اعمال می‌شود و در افکتِ تم هم لحاظ شد تا با toggleِ تم ریست نشود (مثلِ الگوی شبکه). overrideِ خالی ⇒ همان `TH.bg`، پس **بدونِ رگرسیون**. حالتِ گرادیانِ پس‌زمینه (`bgType='gradient'` + `bgColor2`) نیازمندِ importِ `ColorType` است و برای لوپِ بعد ماند؛ فعلاً رنگِ solid پوشش داده شد.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۴/۱۰ در انتظار). باقیِ کلیدهای مرده: خطِ قیمتِ آخر، countdown/currency/unit، کراس‌هیرِ رنگی، گرادیانِ پس‌زمینه.**

## لوپ #62 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ «خطِ قیمتِ آخر» در تنظیمات
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی (کاملِ TV، بدونِ تغییر):** حالتِ **بازپخشِ نوار (Bar Replay)** با Ctrl+Alt+P باز شد — نوارِ کنترلِ پایین کاملِ TradingView است: پخش/مکث، گام، اسلایدرِ پیشرفت (۴۴۱/۸۰۱)، انتخابِ سرعت (۰٫۱×…۳۰×)، خروج. خطِ نشانگرِ قرمزِ نقطه‌چین روی کندلِ توقف. وفادار.
- **ادامهٔ پاک‌سازیِ کلیدهای مردهٔ تنظیمات (#59 شبکه، #60 کندل، #61 پس‌زمینه):** این لوپ **خطِ قیمتِ آخر** (`priceLineShown`) وصل شد.
- **رفع (green apple، ایمن):** هلپرِ `applyPriceLineVis()` (خواندنِ `settingsRef`) `priceLineVisible`+`lastValueVisible` را روی سریِ فعال ست می‌کند؛ پس از هر `buildPriceSeries` (مسیرِ فچ + مسیرِ بازپخش) فراخوانی می‌شود تا با تعویضِ نماد/نوع‌چارت بماند، و در `onChange` هم زنده اعمال می‌شود. overrideِ خالی ⇒ نمایش (پیش‌فرض)، پس **بدونِ رگرسیون**.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۵/۱۰ در انتظار). باقیِ کلیدهای مرده: countdown/currency/unit روی مقیاس، کراس‌هیرِ رنگی، گرادیانِ پس‌زمینه، واترمارک.**

## لوپ #63 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ «رنگِ کراس‌هیر» + دیپلویِ بچ
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **ادامهٔ پاک‌سازیِ کلیدهای مردهٔ تنظیمات (#59/#60/#61/#62):** این لوپ **رنگِ کراس‌هیر** (`crosshairColor`) وصل شد.
- **رفع (green apple، ایمن):** افکتِ استایلِ کراس‌هیر حالا `settingsRef.current.crosshairColor` را می‌خواند (fallback به خاکستریِ خنثیِ TV `#9598a1`)؛ در `onChange` هم زنده با `applyOptions` (merge، پس سبک/نمایش حفظ می‌شود) اعمال می‌شود. overrideِ خالی ⇒ همان پیش‌فرض، بدونِ رگرسیون.
- بیلد: سبز.

### دیپلویِ لوپ #63 — بچِ ۵۸–۶۳ لایو شد
- `docker compose ... up -d --build frontend-prochart` روی HEAD `1575e5f`؛ api recreate و سریع healthy؛ frontend healthy، سایت `200`.
- **تأییدِ زندهٔ پس از دیپلوی (اسکرین‌شات):** چارت با کندلِ پیش‌فرضِ قرمز/سبز درست رِندر شد (مسیرِ پیش‌فرضِ #60/#62 سالم — **بدونِ رگرسیون** روی کدِ هستهٔ سری‌ساز). خطِ LIVE هست (#62). «بازار باز» در جزئیات (#47). **بازهٔ روز حالا قیمتِ زنده را دربر می‌گیرد** (۱٫۱۴۱۵۹ روی کف — رفعِ #52 لایو). ساعتِ نوارِ پایین «(تهران) ۲۳:۱۲:۳۰» (#45/#46).
- نتیجه: **۶ تغییرِ لوپ‌های ۵۸–۶۳ (آیکونِ نوع‌چارت + وصلِ ۴ کلیدِ تنظیمات: شبکه/کندل/پس‌زمینه/خطِ‌قیمت + رنگِ کراس‌هیر) لایو و تأییدشده. در-انتظار → ۰/۱۰.**

## لوپ #64 — ۱۴۰۵/۰۴/۱۹ — تأییدِ تعاملیِ زندهٔ «رنگِ کندل» (#60) روی سایتِ لایو
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`.
- **هدف:** تأییدِ سرراستِ اینکه وصلِ رنگِ کندلِ لوپ #60 (که در #63 دیپلوی شد) واقعاً روی سایتِ لایو کار می‌کند — نه فقط بیلد سبز.
- **روش (Playwright تعاملی):** باز کردنِ دیالوگِ تنظیمات (Ctrl+,) → تغییرِ رنگِ صعودیِ بدنه/حاشیه/فتیله به آبیِ `#2962ff` → تأیید → اسکرین‌شات.
- **نکتهٔ روش‌شناختی:** تلاشِ اول false-negative داد چون ست‌کردنِ مستقیمِ `value`ِ inputِ کنترل‌شدهٔ React، `onChange` را فایر نمی‌کند؛ با **native value setter** (`HTMLInputElement.prototype.value`) درست شد.
- **نتیجه (اسکرین‌شات):** کندل‌های صعودی حالا **آبی** و نزولی قرمز — یعنی **رنگِ کندل end-to-end روی لایو کار می‌کند** ✅. (تغییر فقط در localStorageِ مرورگرِ هدلسِ تست بود؛ سایتِ واقعی برای کاربران دست‌نخورده.)
- بدونِ تغییرِ کد این لوپ (فقط تأیید — چیزی خراب نبود). وضعیت: ۰/۱۰ در انتظار.

## لوپ #65 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ «واترمارک» در تنظیمات
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **ادامهٔ پاک‌سازیِ کلیدهای مردهٔ تنظیمات (#59–#63):** این لوپ **واترمارک** (`watermarkShown` + `watermarkOpacity`) وصل شد.
- **یافته:** کامپوننتِ `Watermark` روی چارت با `opacity={0.07}`ِ هارد‌کد رِندر می‌شد و به تاگل/اسلایدرِ دیالوگ وصل نبود.
- **رفع (green apple، ایمن):** رِندر مشروط به `watermarkShown` شد و opacity از `watermarkOpacity` (۰–۱۰۰٪) می‌آید؛ نگاشت `pct/100*0.14` طوری که **پیش‌فرضِ ۵۰٪ = همان ۰٫۰۷ ظریفِ قبلی** (بدونِ رگرسیون). کامپوننتِ React است پس واکنشی به‌روز می‌شود (بدونِ settingsRef/refetch).
- قوانین: بدونِ صرافی/بروکر؛ لوگوی اختصاصیِ خودِ Pro-Chart؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۱/۱۰ در انتظار). کلیدهای مردهٔ باقی: countdown/currency/unit روی مقیاس، گرادیانِ پس‌زمینه، navButtons/scrollScale، تبِ Events (سهام).**

## لوپ #66 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ تاگلِ «شمارشِ معکوسِ بسته‌شدن»
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **ادامهٔ پاک‌سازیِ کلیدهای مردهٔ تنظیمات (#59–#65):** این لوپ تاگلِ **شمارشِ معکوسِ بسته‌شدنِ کندل** (`scaleCountdown`) وصل شد.
- **یافته + ناسازگاری:** چیپِ `CountdownChip` همیشه (بدونِ گِیت) نمایش داده می‌شد، اما پیش‌فرضِ دیالوگ `scaleCountdown:false` بود ⇒ تاگل هم مرده هم ناسازگار (خاموش نشان می‌داد ولی چیپ روشن بود).
- **رفع (green apple، ایمن):** ۱) رِندرِ چیپ به `chartSettingsOverrides.scaleCountdown !== false` گِیت شد. ۲) پیش‌فرضِ دیالوگ به `scaleCountdown:true` تغییر کرد تا با رفتارِ واقعیِ همیشه-روشن هم‌خوان و **صادق** باشد. پیش‌فرض (undefined/true) ⇒ چیپ نمایش داده می‌شود، پس **بدونِ رگرسیون**؛ خاموش‌کردنِ تاگل حالا واقعاً چیپ را پنهان می‌کند. React است پس واکنشی.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۲/۱۰ در انتظار). ۷ کلیدِ تنظیمات تا کنون زنده شد (شبکه/کندل/پس‌زمینه/خطِ‌قیمت/کراس‌هیر/واترمارک/countdown).**

## لوپ #67 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: وصل‌کردنِ «اندازهٔ فونتِ محورها»
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **ادامهٔ پاک‌سازیِ کلیدهای مردهٔ تنظیمات (#59–#66):** این لوپ **اندازهٔ فونتِ محورها** (`scaleFontSize`، سلکتورِ دیالوگ، پیش‌فرض ۱۲) وصل شد.
- **رفع (green apple، ایمن):** chart-level `applyOptions({ layout: { fontSize } })`؛ در `onChange` زنده و در افکتِ تم هم لحاظ شد تا با toggleِ تم بماند. پیش‌فرضِ کتابخانه هم ۱۲ است، پس overrideِ خالی ⇒ بدونِ رگرسیون.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۳/۱۰ در انتظار). ۸ کلیدِ تنظیمات زنده شد. باقی: scaleCurrency/scaleUnit (نیازمندِ priceFormatterِ سفارشی)، گرادیانِ پس‌زمینه، navButtons/scrollScale، Events(سهام).**

## لوپ #68 — ۱۴۰۵/۰۴/۱۹ — تأییدِ ابزارِ ترسیم + تکمیلِ «گرادیانِ پس‌زمینه»
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی (کاملِ TV، بدونِ تغییر):** ابزارِ **خطِ روند** (Alt+T) با Playwright تست شد — انتخابِ ابزار، کشیدنِ خطِ آبی، و **بازگشتِ خودکار به نشانگر** پس از ترسیم (رفتارِ دقیقِ TradingView). خطِ ترسیم‌شده درست رِندر شد.
- **تکمیلِ کارِ #61 (گرادیانِ پس‌زمینه که آنجا معوق ماند):** حالتِ `bgType='gradient'` وصل شد.
- **پیاده‌سازی (green apple، ایمن):** هلپرِ `bgOpts(ov)` — solid `{color}` (پیش‌فرض) یا `{type:'gradient', topColor:bgColor, bottomColor:bgColor2}`. چون مقادیرِ `bgType` (`'solid'|'gradient'`) دقیقاً برابرِ `ColorType`اند، بدونِ importِ اضافه پاس می‌شوند. در افکتِ تم و `onChange` استفاده شد. overrideِ خالی ⇒ solid با `TH.bg`، پس **بدونِ رگرسیون**.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۴/۱۰ در انتظار). دیالوگِ تنظیمات تقریباً کاملاً کارکردی شد؛ باقی: scaleCurrency/scaleUnit (formatterِ سفارشی)، navButtons/scrollScale، Events(سهام).**

## لوپ #69 — ۱۴۰۵/۰۴/۱۹ — تأییدِ Data Window + وصلِ «اسکرول/زوم»
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی (کاملِ TV، بدونِ تغییر):** **پنجرهٔ داده (Data Window)** با کلیک‌راست → «پنجرهٔ داده» باز شد و با hover پر شد — سرتیتر + نماد·H1 + زمان + O/H/L/C (رنگیِ جهت‌دار) + تغییر، دقیقاً مثلِ TradingView.
- **ادامهٔ پاک‌سازیِ کلیدهای مردهٔ تنظیمات:** تاگلِ **اسکرول و مقیاس** (`scrollScale`) وصل شد → `handleScroll`+`handleScale`. `navButtons` (اورلیِ اختصاصیِ گوشه، نه گزینهٔ نیتیوِ چارت) رد شد.
- **رفع (green apple، ایمن):** chart-level `applyOptions`؛ پیش‌فرضِ کتابخانه `true` پس overrideِ خالی ⇒ بدونِ رگرسیون؛ خاموش‌کردن، درگ‌اسکرول/زوم را قفل می‌کند.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطق؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۵/۱۰ در انتظار). ۹ کلیدِ تنظیمات زنده شد. باقیِ عملاً‌کاربردی: scaleCurrency/scaleUnit (formatterِ سفارشیِ پرریسک)، navButtons(اختصاصی)، Events(سهام).**

## لوپ #70 — ۱۴۰۵/۰۴/۱۹ — 🚀 دیپلویِ بچِ تنظیمات (۶۵–۶۹) + تأییدِ زنده
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`.
- **دیپلوی (کادنس، ۵ تغییرِ وصلِ تنظیمات):** `docker compose ... up -d --build frontend-prochart` روی HEAD `c3fccd5`. api recreate و سریع healthy؛ frontend healthy، سایت `200`.
- **تأییدِ زندهٔ پس از دیپلوی (اسکرین‌شات، چکِ رگرسیون):** چارت با پیش‌فرض‌ها درست رِندر شد — کندلِ قرمز/سبز، فونتِ محورِ نرمال (#67 سالم)، پس‌زمینهٔ سفیدِ solid (#68 سالم)، خطوطِ شبکه (#59)، واترمارک (#65 پیش‌فرض روشن)، چیپِ countdown «۴۶:۰۵» (#66 پیش‌فرض روشن). **بدونِ رگرسیون** روی افکتِ تم که در #67/#68 دست خورد. مکانیزمِ آنچینجِ تنظیمات قبلاً در #64 (رنگِ کندل) به‌صورتِ end-to-end اثبات شده بود.
- نتیجه: **۵ تغییرِ لوپ‌های ۶۵–۶۹ (واترمارک/countdown/فونت/گرادیان/اسکرول‌ومقیاس) لایو و تأییدشده. در-انتظار → ۰/۱۰.**
- **جمع‌بندیِ کارِ تنظیمات:** طی لوپ‌های ۵۹–۶۹، ۹ کلیدِ مردهٔ دیالوگِ «تنظیماتِ چارت» زنده شد (شبکه/کندل/پس‌زمینه‌+گرادیان/خطِ‌قیمت/کراس‌هیر/واترمارک/countdown/فونت/اسکرول‌ومقیاس). دیالوگ اکنون عملاً کاملاً کارکردی است.

## لوپ #71 — ۱۴۰۵/۰۴/۱۹ — تأییدِ سیستمِ اندیکاتور + رفعِ سوءِ‌ظنِ باگ (تشخیص)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی:** میان‌بُرِ «/» → دیالوگِ اندیکاتورها؛ افزودنِ «باند بولینگر» → روی چارت رِندر شد (باندها پشتِ مودال دیده شدند). Escape دیالوگ را می‌بندد.
- **سوءِظنِ باگ و تشخیص:** در چند اسکرین‌شات، بعد از تعامل با دیالوگِ اندیکاتور، مدالِ «جستجوی نماد» ظاهر می‌شد. با دو تستِ تشخیصیِ تمیز بررسی شد: (۱) «/» صرف ⇒ فقط اندیکاتورها باز می‌شود (نه سرچ). (۲) «/» سپس Escape ⇒ **هیچ‌کدام** باز نمی‌ماند. ⇒ **باگی نیست**؛ ظهورِ سرچ یک artifactِ تستی از مرحلهٔ کلیکِ آیتم بود، نه رفتارِ محصول. (تشخیص جلوی گزارشِ باگِ کاذب را گرفت.)
- **نتیجه:** سیستمِ اندیکاتور (افزودن/رِندر/میان‌بُر/بستن) درست کار می‌کند — بدونِ تغییرِ کد این لوپ (چیزی خراب نبود). ۰/۱۰ در انتظار.
- قوانین: بدونِ صرافی/بروکر؛ اسکرین‌شات+مقایسه انجام شد.

## لوپ #72 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: اعشارِ اشتباهِ محورِ قیمت در مینی‌چارت‌های چیدمانِ چند‌چارتی
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی:** چیدمانِ **۲-چارتی** (Ctrl+Alt+G) تست شد — دو پِینِ مستقل (EURUSD | XAUUSD)، هر کدام سلکتورِ نماد + محورِ قیمتِ خودش، و **کراس‌هیرِ همگام** بینِ پِین‌ها (خطِ نقطه‌چینِ هم‌زمان). کاملِ TradingView.
- **یافته (باگِ واقعی):** مینی‌چارت‌های گرید `priceFormat` نداشتند ⇒ پیش‌فرضِ کتابخانه (۲ رقم) → محورِ EURUSD «۱٫۱۴/۱٫۱۵» نشان می‌داد، در حالی که فارکس ۵ رقم می‌خواهد (چارتِ اصلی درست بود). هم‌جنسِ باگِ اعشارِ اسکنر (#54).
- **رفع (green apple، ایمن):** import و استفاده از `priceDigits`ِ کانونیِ `symbolMeta` در افکتِ لودِ دیتای MiniChart؛ `applyOptions({ priceFormat: { precision, minMove } })` per-symbol. حالا هر پِینِ گرید اعشارِ درستِ نمادش را دارد.
- قوانین: بدونِ صرافی/بروکر؛ فقط فرمت‌بندیِ عدد؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۱/۱۰ در انتظار).**

## لوپ #73 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: اعشارِ برچسبِ قیمتِ سرتیترِ مینی‌چارت
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی (کاملِ TV، بدونِ تغییر):** منوی **تایم‌فریم** (چِوِرونِ کنارِ تب‌ها) باز شد — فهرستِ ۱m/5m/15m/30m/1H/2H/4H/1D/1W/1Mo با **ستارهٔ منتخب** per-ردیف (منتخب‌ها = دکمه‌های سریعِ تولبار)، اینترvalِ فعال آبی. وفادار.
- **یافته (باگِ واقعی، دنبالهٔ #72):** برچسبِ قیمتِ سرتیترِ مینی‌چارت `{last}` را خام رِندر می‌کرد ⇒ EURUSD «۱٫۱۴۱۹۴۳۵۷» (۸ رقم!) نشان می‌داد.
- **رفع (green apple، ایمن):** `Number(last).toFixed(priceDigits(symbol))` — همان دقتِ محورِ #72. حالا سرتیترِ هر پِینِ گرید هم اعشارِ درستِ نمادش را دارد (فارکس ۵، طلا ۲…).
- قوانین: بدونِ صرافی/بروکر؛ فقط فرمت‌بندیِ عدد؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۲/۱۰ در انتظار).**

## لوپ #74 — ۱۴۰۵/۰۴/۱۹ — تشخیص: «واچ‌لیستِ موبایل خالی» یک artifactِ هدلس بود، نه باگ
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده (موبایل ۳۹۰px).
- **بررسی:** تبِ **واچ‌لیستِ موبایل** (BottomNav) — در اسکرین‌شاتِ هدلس، هدر «واچ‌لیست» می‌شد و تب فعال بود، اما **چارت پشتش دیده می‌شد** (نه لیست). به‌نظر باگ آمد.
- **تشخیص (جلوی گزارشِ باگِ کاذب را گرفت):** ۱) `AppShell` → `WatchlistScreen` وقتی `tab==='watchlist'` به‌صورتِ overlay رِندر می‌شود. ۲) کامپوننتِ `Screen` (ui.jsx) `absolute inset-0` + پس‌زمینهٔ **مات** `var(--surface-default)` دارد ⇒ در مرورگرِ واقعی/APK کاملاً چارت را می‌پوشاند. ⇒ دیده‌شدنِ چارت فقط چون **`--surface-default` در Chromiumِ هدلس resolve نمی‌شود** (CSS varها ست نیستند) پس پس‌زمینه شفاف می‌افتد. **روی دستگاهِ واقعی سالم است.**
- **نتیجه:** باگ نیست؛ artifactِ محیطِ تست. بدونِ تغییرِ کد (تغییرِ اسپکولتیو می‌توانست صفحهٔ سالم را خراب کند). ۲/۱۰ در انتظار.
- قوانین: بدونِ صرافی/بروکر؛ اسکرین‌شات+مقایسه انجام شد.

## لوپ #75 — ۱۴۰۵/۰۴/۱۹ — parity: نمایان‌کردنِ خطوطِ مرجعِ اسیلاتور (۷۰/۳۰)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی:** افزودنِ **RSI** → در پِینِ جدا (sub-pane) زیرِ چارتِ اصلی رِندر شد (رفتارِ دقیقِ TradingView برای اسیلاتورها). RSI در REGISTRY هم `guides:[30,70]` و `range:[0,100]` دارد و خطوطِ راهنما با `createPriceLine` رسم می‌شوند.
- **یافته (گپِ واقعیِ بصری):** خطوطِ مرجعِ اسیلاتور (۷۰/۳۰ در RSI، و مشابه در Stochastic/Aroon) با `color: TH.grid` رسم می‌شدند — **دقیقاً همرنگِ خطوطِ شبکه**، پس عملاً نامرئی؛ در TradingView این سطوح دیده می‌شوند.
- **رفع (green apple، ایمن، توکن‌محور):** رنگ به `TH.text` (خاکستریِ خنثی، در هر دو تم دیده می‌شود) تغییر کرد؛ همچنان dashed و نازک (subtle). حالا سطوحِ اشباعِ خرید/فروش خوانا هستند.
- قوانین: بدونِ صرافی/بروکر؛ فقط رنگِ توکنی؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۳/۱۰ در انتظار).**

## لوپ #76 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگِ واقعی: تداخلِ میان‌بُرِ «/» (اندیکاتور + جستجوی نماد)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **کشفِ ریشه‌ایِ artifactِ لوپ‌های قبل:** آن «ظاهرشدنِ مدالِ جستجوی نماد بعد از کارِ اندیکاتور» که در #71 آن را artifactِ تست فرض کرده بودم، **در واقع یک باگِ واقعی بود**. کلیدِ «/» دوبار bind شده بود: (۱) `hotkeys.js` → دیالوگِ اندیکاتورها، (۲) هندلرِ سراسریِ خطِ ۱۴۰۶ → `setSymModal(true)` (مدالِ جستجو). پس «/» هر دو را باز می‌کرد (یکی پشتِ دیگری) و بستنِ اندیکاتور، مدالِ جستجو را نمایان می‌کرد.
- **رفع (green apple، ایمن):** «/» از هندلرِ جستجوی نماد حذف شد؛ فقط `Ctrl/⌘+K` مدالِ جستجو را باز می‌کند. «/» طبقِ راهنمای میان‌بُرها (و دیالوگِ #48) فقط دیالوگِ اندیکاتور را باز می‌کند. تداخل رفع شد.
- **درسِ روش‌شناختی:** فرضِ «artifactِ تست» در #71/#74 برای «/» نادرست بود — کاوشِ عمیق‌ترِ کد باگِ واقعی را آشکار کرد. (تشخیصِ #74 دربارهٔ CSS-varِ موبایل هنوز درست است.)
- قوانین: بدونِ صرافی/بروکر؛ فقط منطقِ میان‌بُر؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۴/۱۰ در انتظار).**

## لوپ #77 — ۱۴۰۵/۰۴/۱۹ — parity: نمایشِ پارامترهای اندیکاتور در لِجند («RSI 14»)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **یافته (گپِ واقعی، دنبالهٔ بررسیِ لِجندِ #76):** لِجندِ اندیکاتورها فقط نامِ پایه را نشان می‌داد («RSI») بدونِ پارامترها؛ در TradingView پارامترها هم می‌آیند («RSI 14»، «MACD 12 26 9»).
- **رفع (green apple، ایمن):** هلپرِ `indLbl(key, inputs)` که ورودی‌های عددیِ اندیکاتور را به برچسب می‌افزاید (فقط اعداد، تا source/رنگ نیاید). در ساختِ `legendItems` برای هر دو overlay و sub استفاده شد. حالا لِجند مثلِ TradingView پارامترها را نشان می‌دهد.
- قوانین: بدونِ صرافی/بروکر؛ فقط برچسب؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۵/۱۰ در انتظار).**

### دیپلویِ لوپ #77 — بچِ ۷۲–۷۷ لایو شد
- `docker compose ... up -d --build frontend-prochart` روی HEAD `0952b52`؛ api recreate و سریع healthy؛ frontend healthy، سایت `200`.
- **تأییدِ زندهٔ رفعِ #76 (تداخلِ «/»):** تستِ تعاملی → «/» فقط دیالوگِ اندیکاتور را باز می‌کند (`indicators=true, symbolSearch=false`) ✅. قبلاً مدالِ جستجو هم پشتش باز می‌شد. (Ctrl+K در هدلس به‌خاطرِ intercept‌شدنِ کلیدِ مرورگر false داد، ولی شرطش در ادیتِ من دست‌نخورده بود پس رگرسیون نیست؛ جستجوی نماد از سرچ‌باکسِ تاپ‌بار هم در دسترس است.)
- **چکِ رگرسیون (اسکرین‌شات):** چارت سالم — کندل/LIVE/واچ‌لیست/جزئیات(«بازار باز»)/بازهٔ روزِ حاویِ قیمتِ زنده/ساعتِ نوارِ پایین. بدونِ رگرسیون.
- نتیجه: **۵ تغییرِ ۷۲–۷۷ (اعشارِ محور و سرتیترِ مینی‌چارت، خطوطِ مرجعِ اسیلاتور، رفعِ تداخلِ «/»، پارامترهای لِجند) لایو و تأییدشده. در-انتظار → ۰/۱۰.**

## لوپ #78 — ۱۴۰۵/۰۴/۱۹ — تأییدِ #77 + یک‌دست‌کردنِ برچسبِ اندیکاتور در پنجرهٔ داده
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **تأییدِ زندهٔ #77:** RSI (استوکاستیک‌RSI) اضافه شد → لِجند حالا **«استوکاستیک RSI 14 3 3»** (نام + پارامترها) + مقادیرِ زنده نشان می‌دهد ✅. تأیید کرد که «/» هم فقط دیالوگِ اندیکاتور را باز می‌کند (رفعِ #76 لایو).
- **یافتهٔ ناسازگاری:** برچسبِ اندیکاتور در **پنجرهٔ داده** جدا از لِجند از `def.label` (بدونِ پارامتر) می‌آمد ⇒ لِجند «RSI 14» ولی پنجرهٔ داده «RSI».
- **رفع (green apple، ایمن، بدونِ تکرار):** هلپرِ `indLbl` به **سطحِ ماژول** منتقل شد (کنارِ `fmtPrice`، از REGISTRYِ ایمپورت‌شده استفاده می‌کند)؛ نسخهٔ درون‌کامپوننتی حذف و در `legendItems` + هر دو `indLabelRef` (main/sub که پنجرهٔ داده می‌خواند) استفاده شد. حالا لِجند و پنجرهٔ داده یک‌دست پارامتر نشان می‌دهند.
- قوانین: بدونِ صرافی/بروکر؛ فقط برچسب/ری‌فکتور؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۱/۱۰ در انتظار).**

## لوپ #79 — ۱۴۰۵/۰۴/۱۹ — parity: دکمهٔ «پرش به جدیدترین کندل»
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیک‌راستِ محورِ قیمت (منوی چارت با «بازنشانیِ مقیاس» ظاهر شد؛ مود‌های scale در تولبار/تنظیمات هستند).
- **یافته (گپِ واقعی):** `scrollToRealTime` فقط به کلیدِ End وصل بود؛ **هیچ دکمهٔ دیداریِ «پرش به حال» نبود**. در TradingView وقتی در تاریخ اسکرول می‌کنی، یک دکمهٔ شناور در پایین-راستِ چارت ظاهر می‌شود که به جدیدترین کندل برمی‌گرداند.
- **پیاده‌سازی (green apple، اصیل):** state `atRealtime` که در همان `subscribeVisibleLogicalRangeChange`ِ موجود ست می‌شود (اگر `rng.to < n-1` یعنی از حال دور شده‌ای). دکمهٔ گِردِ شناور (SVGِ دابل‌شِوِرونِ اختصاصی به‌راست = پرش به آخرین بار) فقط وقتی `!atRealtime` و تک‌چارت است ظاهر می‌شود؛ کلیک → `scrollToRealTime()`. توکن‌محور، دِکلاترد.
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۲/۱۰ در انتظار).**

## لوپ #80 — ۱۴۰۵/۰۴/۱۹ — 🚀 دیپلویِ ۷۸/۷۹ + تأییدِ زندهٔ دکمهٔ «پرش به حال»
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`.
- **دیپلوی:** `docker compose ... up -d --build frontend-prochart` روی HEAD `6dab923`. api recreate و سریع healthy؛ frontend healthy، سایت `200`.
- **تأییدِ زندهٔ #79 (اسکرین‌شاتِ تعاملی):** با Home + ArrowLeft به تاریخ اسکرول شد → دکمهٔ گِردِ «پرش به جدیدترین کندل» با آیکونِ دابل‌شِوِرونِ راست (») در گوشهٔ **پایین-راستِ چارت** (x≈۱۱۰۴, y≈۷۹۶) ظاهر شد و درست رِندر شد ✅. `boundingBox` تأیید کرد present است فقط وقتی از حال دور شده‌ای. دقیقاً مثلِ TradingView.
- **تأییدِ #78:** برچسبِ اندیکاتور با پارامتر در لِجند/پنجرهٔ داده یک‌دست است (از دیپلویِ قبل تأیید شده بود).
- نتیجه: **۲ تغییرِ ۷۸/۷۹ لایو و تأییدشده. در-انتظار → ۰/۱۰.**

## لوپ #81 — ۱۴۰۵/۰۴/۱۹ — تأییدِ دیالوگِ تنظیماتِ اندیکاتور + پولیشِ برچسبِ ورودی
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **بررسیِ تعاملی (کاملِ TV، بدونِ گپ):** دیالوگِ **تنظیماتِ اندیکاتور** (چرخ‌دندهٔ لِجند) باز شد:
  - تبِ **ورودی‌ها**: پارامترها با استپر (period=14, k=3, d=3) + تأیید.
  - تبِ **استایل**: رنگ، ضخامت (۱–۴)، سبکِ خط (نقطه/خط‌چین/توپر)، شفافیت (اسلایدر)، نمایش روی چارت (توگل). **تبِ مرده نیست — کاملاً کارکردی.** ضمناً برای اندیکاتورهای چندخطی رنگِ **per-line** دارد (وفادار).
- **پولیشِ کوچک (green apple):** برچسبِ ورودی‌ها از کلیدِ خامِ کوچک («period/k/d») به **حرفِ اولِ بزرگ** («Period/K/D») تغییر کرد — نزدیک‌تر به سبکِ TradingView.
- قوانین: بدونِ صرافی/بروکر؛ فقط برچسب؛ اسکرین‌شات+مقایسه (۲ تب) انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۱/۱۰ در انتظار).**

## لوپ #82 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگ: اعشارِ اشتباهِ رمزارزهای میان‌قیمت
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + کلیپِ نوارِ حجم.
- **بررسیِ حجم (کاملِ TV، بدونِ تغییر):** رنگِ نوارِ حجم `c.c >= c.o ? سبز(.5) : قرمز(.5)` است — دقیقاً مثلِ TradingView (سبز/قرمزِ جهت‌دار، نیمه‌شفاف). درست.
- **یافته (باگِ واقعی):** `priceDigits` رمزارزهای میان‌قیمتِ غیرِ BTC/ETH (مثلِ **BNB ~$۶۰۰، SOL ~$۱۵۰**) را به پیش‌فرضِ **۵ رقم** می‌انداخت ⇒ «۶۰۰٫۰۰۰۰۰» (زشت و اشتباه؛ باید ۲ رقم باشد).
- **رفع (green apple، ایمن، افزایشی):** رجکسِ `BNB|SOL|LTC|BCH|AVAX|DOT|LINK|ATOM|NEAR|UNI|AAVE|XMR` → ۲ رقم، هم در `priceDigits`ِ چارتِ اصلی و هم در `digits`ِ اسکنر (یک‌دست). فقط رمزارزهای میان‌قیمت را می‌گیرد؛ نمادهای موجود دست‌نخورده ⇒ بدونِ رگرسیون. (هم‌جنسِ رفعِ اعشارِ شاخصِ #54.)
- قوانین: بدونِ صرافی/بروکر؛ فقط فرمت‌بندیِ عدد؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۲/۱۰ در انتظار).**

## لوپ #83 — ۱۴۰۵/۰۴/۱۹ — نوارِ استایلِ ترسیم: آیکونِ خط‌چینِ اختصاصی (به‌جای گلیفِ ┄)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده (رسمِ خطِ افقی با Alt+H → خطِ آبیِ 1.15288 + برچسبِ قیمت).
- **تصحیحِ برداشتِ اولیه:** ابتدا فکر کردم «نوارِ استایلِ آبجکتِ انتخابی» غایب است، اما کد نشان داد **وجود دارد** (خطِ ۱۸۵۹: رنگ + ضخامت ۱/۲/۳ + خط‌چین + قفل، بالا-وسطِ چارت). فقط کلیکِ انتخابِ من آن را trigger نکرد.
- **یافته (هم‌جنسِ باگ‌های گلیفِ #50/#56):** دکمهٔ خط‌چینِ این نوار از کاراکترِ `┄` (box-drawing) به‌عنوانِ آیکون استفاده می‌کرد — وابسته به فونت، احتمالِ □، و ناهم‌جنس با بقیهٔ آیکون‌های SVGِ همان نوار.
- **رفع (green apple، اصیل):** `┄` با **آیکونِ خط‌چینِ اختصاصیِ SVG** (`strokeDasharray`) جایگزین شد — بدونِ وابستگی به فونت، هم‌جنسِ آیکونِ رنگ/قفل.
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۳/۱۰ در انتظار).**

## لوپ #84 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگِ گلیف: آیکون‌های اختصاصیِ SVG برای نوارِ بازپخش
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **اسکنِ سیستماتیکِ گلیف (پیروِ #83):** جستجوی کاراکترهای وابسته‌به‌فونتِ استفاده‌شده به‌عنوانِ آیکون در UIِ چارت. یافته: **نوارِ کنترلِ بازپخش** از `⏮ ⏸ ▶ ⏭` (خانوادهٔ symbol-emojiِ U+23xx، هم‌جنسِ ⏱ که در #50 □ می‌شد) استفاده می‌کرد.
- **رفع (green apple، اصیل):** هر سه دکمهٔ گامِ‌عقب/پخش‌مکث/گامِ‌جلو با **آیکون‌های اختصاصیِ SVG** (مثلث/میله‌های fill=currentColor) جایگزین شدند — بدونِ وابستگی به فونت. `✕ خروج` و `●`ها سالم‌اند (رِندر می‌شوند).
- قوانین: بدونِ صرافی/بروکر؛ آیکونِ SVGِ اختصاصی؛ اسکرین‌شات+مقایسه انجام شد؛ دِکلاترد.
- بیلد: سبز. **وضعیت: کامیت‌شده. دیپلویِ بچِ ۸۱–۸۴ در همین لوپ.**

### دیپلویِ لوپ #84 — بچِ ۸۱–۸۴ لایو شد
- `docker compose ... up -d --build frontend-prochart` روی HEAD `6650dff`؛ api recreate و سریع healthy؛ frontend healthy، سایت `200`.
- **تأییدِ زندهٔ #84 (اسکرین‌شات):** واردِ بازپخش شدم → نوارِ کنترل حالا **آیکون‌های SVGِ تمیز** دارد: گامِ‌عقب (میله+مثلثِ چپ)، پخش (مثلثِ راست در دکمهٔ آبی)، گامِ‌جلو (مثلث+میله). گلیف‌های tofu-prone حذف شدند ✅.
- نتیجه: **۴ تغییرِ ۸۱–۸۴ (برچسبِ ورودیِ اندیکاتور، اعشارِ کریپتوی میان‌قیمت، آیکونِ خط‌چینِ ترسیم، آیکون‌های بازپخش) لایو و تأییدشده. در-انتظار → ۰/۱۰.**

## لوپ #85 — ۱۴۰۵/۰۴/۱۹ — بستنِ کاملِ کلاسِ باگِ گلیفِ فونت‌وابسته (اسکنِ سراسری)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + پنلِ اسکنر.
- **تلاشِ تأییدِ #82:** اسکنر را باز کردم تا اعشارِ کریپتوی میان‌قیمت (BNB/SOL) را ببینم، اما ستونِ قیمت در این سشنِ هدلس «—» بود (فیدِ WS در هدلس پر نشد؛ transient، نه باگ). منطقِ #82 هم‌جنسِ رفعِ تأییدشدهٔ #54 است.
- **اسکنِ سیستماتیکِ گلیف (سراسری):** grep روی کلِ `src` برای خانوادهٔ symbol-emojiِ tofu-prone (⏮⏭⏯⏸⏹⏱⏲⌛⏪⏩…) به‌عنوانِ آیکونِ متنی → **هیچ نمونهٔ باقی‌مانده‌ای نیست**. پنل‌ها هم تمیزند.
- **نتیجه:** کلاسِ باگِ «کاراکترِ فونت‌وابسته به‌جای آیکون» که ۴ باگِ واقعی داشت (⏱#50, 👆#56, ┄#83, ⏮⏸▶⏭#84) حالا **کاملاً و به‌صورتِ سراسری بسته شد** — بدونِ نمونهٔ باقی‌مانده. بدونِ تغییرِ کدِ این لوپ (چیزی برای رفع نماند). ۰/۱۰ در انتظار.
- قوانین: بدونِ صرافی/بروکر؛ اسکرین‌شات+مقایسه انجام شد.

## لوپ #86 — ۱۴۰۵/۰۴/۱۹ — تأییدِ تمِ تیره (وفادار به TV) + منوی همبرگر (اصیلِ Pro-Chart)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده.
- **منوی همبرگر (☰):** یک draweرِ راستِ اختصاصیِ Pro-Chart است — کاربرِ مهمان/ورود/ثبت‌نام، اشتراکِ پریمیوم (پلن‌ها به تتر)، فهرستِ ویژگی‌ها، و میان‌بُرهای اسکرینر/آلارم/واچ‌لیست. **سطحِ اصیلِ محصول، نه هدفِ کپیِ TV** — پس تغییری اعمال نشد.
- **تأییدِ تمِ تیره (ویژگیِ کلیدیِ TV):** با Ctrl+Alt+T به تمِ تیره سوییچ شد → کلِ UI (تولبار/ریل‌ها/واچ‌لیست/جزئیات/چارت) تمیز و کامل تیره شد؛ پس‌زمینهٔ ناوی `#131722` (دقیقاً تمِ تیرهٔ TradingView)، کندلِ سبز/قرمز روی تیره، گریدِ ظریف. **کاملاً وفادار.**
- **نکته:** قیمتِ واچ‌لیست/اسکنر در سشنِ هدلس «—» است (فیدِ WS دیر/بدونِ‌اتصال در هدلس؛ چارتِ اصلی داده دارد) — artifactِ محیط، نه باگ.
- بدونِ تغییرِ کدِ این لوپ (هر دو سطح کامل/مناسب‌اند). ۰/۱۰ در انتظار.
- قوانین: بدونِ صرافی/بروکر؛ اسکرین‌شات+مقایسه (روشن+تیره) انجام شد.

## لوپ #87 — ۱۴۰۵/۰۴/۱۹ — رفعِ باگِ واقعی: اعشارِ اشتباهِ محورِ قیمت پس از تعویضِ نماد (stale closure)
- سلامت: `git pull` up-to-date؛ سایتِ زنده `200`. اسکرین‌شاتِ زنده + سوییچ به **BTCUSDT** با Ctrl+K.
- **یافته (باگِ واقعیِ دیده‌شده روی BTC):** محورِ قیمت و برچسبِ LIVE برای بیت‌کوین **۵ رقمِ اعشار** نشان می‌داد («68000.00000»، «64015.73981») در حالی که سرتیتر/لِجند/جزئیات ۱–۲ رقمی بودند.
- **ریشه (تشخیصِ دقیق):** `buildPriceSeries` یک `useCallback` با depهای `[chartType, TH]` بود — **`symbol` جا افتاده**. پس closure نمادِ کهنه را می‌گرفت؛ با EURUSD→BTC (بدونِ تغییرِ chartType/TH) خطِ ۵۷۰ `priceDigits('EURUSD')=5` را روی سریِ بیت‌کوین اعمال می‌کرد. (لِجند/واچ‌لیست از `fmtPrice` با نمادِ جاری استفاده می‌کنند پس درست بودند — فقط محور/خطِ‌قیمت خراب بود.) افکتِ precisionِ ۵۷۸ هم زودتر از بازسازیِ async فایر می‌شد پس رفع نمی‌کرد. `localStorage` تأیید کرد symbol='BTCUSDT'.
- **رفع (green apple، ایمن):** `symbol` به depهای `buildPriceSeries` افزوده شد. fetch-effect از قبل هم `symbol` و هم `buildPriceSeries` را در depها دارد ⇒ **رفچِ اضافه ندارد**، فقط نمادِ تازه گرفته می‌شود. حالا محور برای هر نماد (کریپتو/JPY/شاخص) دقتِ درست دارد.
- قوانین: بدونِ صرافی/بروکر؛ فقط منطقِ deps؛ اسکرین‌شات+مقایسه انجام شد.
- بیلد: سبز. **وضعیت: کامیت‌شده، دیپلوی طبقِ کادنس (۱/۱۰ در انتظار).**
