# 🗺️ نقشهٔ زیرساختِ Pro-Chart (خروجیِ فازِ M1)

> وضعیت: ⚠️ **پیش‌نویس — ممیزی در جریان است.** موجودیِ هتزنر کامل شد؛ ممیزیِ داخلیِ سرورها منتظرِ افزوده‌شدنِ کلیدِ SSH توسطِ مالک.
> همهٔ بررسی‌ها ۱۰۰٪ فقط-خواندنی بوده‌اند. هیچ تغییری روی هیچ سروری اعمال نشده.

## موجودیِ هتزنر (۲۰۲۶-۰۷-۰۱) — ۶ سرور

| نامِ هتزنر | نقشِ حدسی | id | سایز | IP عمومی | SSH | وضعیتِ ممیزی |
|---|---|---|---|---|---|---|
| TraydeYar-Bot | کریپتو (سرور ۱؟) — «تریدیار» | 138390751 | cpx52 — ۱۲c/۲۴GB/۴۸۰GB | 91.107.245.191 | پورت **1367** باز | ⏳ منتظرِ کلید |
| CoinePro-FX | فارکس + آکادمی (سرور ۲) | 133765432 | cpx42 — ۸c/۱۶GB/۳۲۰GB | 91.107.160.235 | پورت **1367** باز | ⏳ منتظرِ کلید |
| CoineproFX-Win-Base | ویندوز / MT5 (سرور ۳) | 144735169 | cx43 — ۸c/۱۶GB/۱۶۰GB | 91.107.186.20 | SSH بسته (RDP؟) | ⏳ روشِ ممیزی: `server-audit.ps1` |
| Po-Chart | هابِ اپ (سرور ۴) — `pro-chart.com` | 143926687 | cpx42 — ۸c/۱۶GB/۳۲۰GB | 91.107.181.124 | پورت **1367** باز | 🟡 از ریپو مستند؛ داخلی منتظرِ کلید |
| App-Central-Server | مرکزی (سرور ۵) | 146981310 | cpx42 — ۸c/۱۶GB/۳۲۰GB | 91.107.183.210 | پورت **1367** | ✅ ممیزی شد |
| Germani-Server | **❓ نامشخص — از مالک پرسیده شود** | 142802373 | cx23 — ۲c/۴GB/۴۰GB | 91.107.179.84 | پورت **22** باز | ⏳ منتظرِ کلید |

همهٔ سرورها Ubuntu 24.04 (به‌جز ویندوز)، وضعیت: running.

### ⚠️ یافته‌های امنیتیِ سطحِ ابر (فقط گزارش — تغییری داده نشد)

1. **هیچ Firewall ابریِ هتزنر تعریف نشده** — همهٔ پورت‌های باز مستقیم روی اینترنت‌اند.
2. **هیچ شبکهٔ خصوصیِ هتزنر (Cloud Network) وجود ندارد** — ترافیکِ بینِ سرورها الان از اینترنتِ عمومی می‌رود. (استراتژیِ سند: باید شبکهٔ خصوصی ساخته شود — نیازمندِ تأییدِ مالک در فازِ بعد.)
3. ولیوم و Load Balancer هم وجود ندارد (بکاپِ جدا از دیسکِ سرور نیست).
4. یک کلیدِ SSH در پنل ثبت است: `prochart-rescue`.

## سرور ۵ — مرکزی (همین سرور) ✅

- **میزبان:** `App-Central-Server` — Ubuntu 24.04.4 LTS، کرنل 6.8
- **منابع:** ۸ هسته / ۱۶GB رم / ۳۰۰GB دیسک (~۱٪ استفاده) — جا برای PostgreSQL مرکزی + سرویسِ Auth کاملاً کافی
- **سرویس‌های در حالِ اجرا:** هیچ سرویسِ کاربردی‌ای نصب نیست؛ **docker نصب نیست** (برای M2 باید نصب شود — با تأییدِ مالک)
- **پورت‌های شنونده:** فقط SSH روی پورتِ **۱۳۶۷** (پورتِ غیرپیش‌فرض — خوب)
- **cron:** خالی. **nginx:** نصب نیست.
- ریپو در `/home/Hub/pro-chart` روی برنچِ `claude/android-apk-project-build-l9a960`
- کاربرِ عاملِ Claude: `Hub` (بدونِ sudo — اصلِ حداقلِ دسترسی رعایت است)

## سرور ۴ — پرو-چارت (هاب) 🟡

**از روی ریپو (`docker-compose.prochart.yml` + `nginx/nginx.conf`):**

| سرویس | ایمیج | پورتِ بیرونی |
|---|---|---|
| timescaledb | timescale/timescaledb:latest-pg17 | 127.0.0.1:5432 (فقط لوکال) |
| redis | valkey/valkey:8-alpine | 127.0.0.1:6379 (فقط لوکال) |
| api (FastAPI) | build از `src/` | 127.0.0.1:8000 (پشتِ nginx → `/api`) |
| frontend-prochart (nginx) | build از `frontend/prochart/` | **80 / 443** |
| data-feed | build | — |
| crypto-ws | build | — |
| news-worker | build | — |
| claude-llm (sidecar) | build از `llm_service/` | — |

- nginx علاوه بر فرانت/API، یک مسیرِ **TLS passthrough با `ssl_preread`** برای مدیا سرورِ WebRTC (لایو ترید) دارد.
- **بررسیِ بیرونی (۲۰۲۶-۰۷-۰۱):** `https://pro-chart.com` → HTTP 200؛ `https://pro-chart.com/api/health` → HTTP 200 (API بالاست)؛ `/api/docs` → 404 (احتمالاً عمداً بسته).
- ولیوم‌ها: `tsdb_data`, `redis_data`, `llm_creds`.

## سرور ۲ — فارکس (بررسیِ بیرونی)

- `https://fx.trade-future.ir` → HTTP 200 (بالاست).

## شبکه / DNS

- **هر دو دامنه پشتِ Cloudflare هستند** (`pro-chart.com` و `fx.trade-future.ir` به IPهای 2606:4700::/32 resolve می‌شوند) → IP واقعیِ سرورها از بیرون مخفی است (خوب)؛ IPهای واقعی از هتزنر API در جدولِ بالا آمده.
- شبکهٔ خصوصیِ هتزنر بینِ سرورها: **وجود ندارد** — پیشنهادِ فازِ بعدی (با تأییدِ مالک): ساختِ Cloud Network و بردنِ ترافیکِ داخلی روی آن + فایروالِ ابری.

---
*آخرین به‌روزرسانی: ۲۰۲۶-۰۷-۰۱ — Claude (سرورِ مرکزی)، فازِ M1 در جریان.*
