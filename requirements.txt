# FastAPI & Web
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.27
websockets==14.1

# Telegram Bot
aiogram==3.15.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1
psycopg2-binary==2.9.10

# Redis
redis[hiredis]==5.2.1

# Celery
celery[redis]==5.4.0

# Authentication
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Data Validation
pydantic==2.9.2
pydantic-settings==2.6.1

# Data Sources
# MetaTrader5==5.0.4621  # Windows-only, not available on Linux
oandapyV20==0.7.2
twelvedata==1.2.28
# yfinance 0.2.50 با تغییرات API یاهو می‌شکند (JSONDecodeError)؛ 1.4.1 + curl_cffi رفعش می‌کند
yfinance==1.4.1
curl_cffi==0.15.0

# Technical Analysis — pinned for numba/talib compat
numpy==2.2.6
pandas==2.2.3
TA-Lib==0.4.32

# Machine Learning
xgboost==2.1.3
lightgbm==4.5.0
torch==2.5.1
optuna==4.1.0
scikit-learn==1.6.0
joblib==1.4.2

# Logging & Monitoring
structlog==24.4.0
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.2

# System Monitoring
psutil==6.1.1

# Utilities
httpx==0.28.1
aiohttp>=3.10.11,<3.11  # سقف به‌خاطرِ aiogram 3.15؛ آخرین نسخهٔ امنِ این بازه
python-dotenv==1.2.2
orjson==3.11.6
jinja2==3.1.6
apscheduler==3.10.4

# Security: HTML sanitization (XSS prevention for user/article content)
bleach==6.2.0

# Data quality (Pandera) — اعتبارسنجیِ اسکیمای کندل (warn-only)
pandera>=0.18,<0.21

# Performance — uvloop (حلقهٔ async سریع‌تر؛ در entrypointها با fast_loop فعال می‌شود).
# نکته: optuna==4.1.0 و TA-Lib==0.4.32 و pandas-ta از قبل بالاتر در همین فایل هستند.
uvloop>=0.19,<0.22

# MLflow client (skinny — سبک، فقط لاگ/ترکینگ از داخلِ trainer)
mlflow-skinny>=2.12,<2.18
feedparser==6.0.11
