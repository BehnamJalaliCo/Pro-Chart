// بازارنما — راهنمای اندیکاتورهای منتقل‌شده از TradingView (ترجمهٔ فارسیِ سلیس + عکس‌های TV، بدون ویدیو).
// این فایل با override روی ورودی‌های قدیمیِ ind_*.js می‌نشیند (در index.js آخر spread می‌شود).
// داده از tv_ind.json (انباشتگرِ خودکارِ خط تولید) خوانده می‌شود.
import data from './tv_ind.json';
export default data;
